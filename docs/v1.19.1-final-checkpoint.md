# v1.19.1 Final Checkpoint

> 日期：2026-05-16
> 性质：Release Hygiene — 无新功能

## 本次修复核心

**v1.19 期间 `.env.production` DATABASE_URL 被错误配置为 Neon，导致：**
- 生产 runtime 连接到空 Neon 库（无邮编数据）
- migration 跑到 Neon 而非本地 PG

**v1.19.1 修复：**
- DATABASE_URL 改回 `postgresql://bxb_user:...@127.0.0.1:5432/bxb_prod`
- ai_usage_logs migration 正确应用到本地 PG
- 全部 AI 额度规则经过真实黑盒测试
- 新增防呆文档 `database-source-of-truth.md`

## 生产主库

- **数据库**：VPS 本地 PostgreSQL `bxb_prod`
- **Host**：127.0.0.1:5432
- **邮编库行数**：2,763,943（关键校验指标）
- **ai_usage_logs 表**：存在，本地 PG

## Neon 状态

- **不是生产主库**
- v1.19 期间写入的少量测试数据视为脏数据
- 未来如需迁移到 Neon，需单独做完整迁移项目

## 恢复注意事项

1. `.env.production` 不进 Git，恢复时必须手动确认 `DATABASE_URL` 指向本地 PG
2. 验证方法：`curl -s "https://jueshi.net/api/postal-codes?country=CA&q=V6B0A1"` 必须返回 Vancouver
3. 邮编库行数必须为 2,763,943
4. 禁止为解决新表缺失问题直接切 DATABASE_URL 到 Neon

## 文件一致性

本地 vs VPS 7 个核心文件 SHA256 完全匹配：
- src/app/api/ai/generate/route.ts ✅
- prisma/schema.prisma ✅
- prisma/migrations/20260517000000_add_ai_usage_log/migration.sql ✅
- docs/database-source-of-truth.md ✅
- src/app/(public)/ai-tools/product-copy/page.tsx ✅
- src/app/(public)/ai-tools/translate-polish/page.tsx ✅
- src/app/(public)/ai-tools/document-summary/page.tsx ✅

## 部署模式

- rsync 文件同步（非 git push）
- VPS HEAD 与本地 HEAD hash 不同属预期行为
- 以 SHA256 文件一致性为准
- v1.19.1-stable tag 仅存在于本地，VPS 需手动创建

## AI 工具状态

- AI_ENABLED=true
- AI_MOCK=true（生产当前为 mock 模式，不消耗真实模型费用）
- 等待配置真实 AI_API_KEY 后可切换 AI_MOCK=false
