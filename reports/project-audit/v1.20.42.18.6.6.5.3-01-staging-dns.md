# Staging DNS Verification — v1.20.42.18.6.6.5.3-01

**Date:** 2026-06-23
**CURRENT_MODE:** DEV
**TARGET:** i.jueshi.net → 192.129.155.149

## DNS Resolution

| Check | Result |
|-------|--------|
| dig i.jueshi.net A | 198.18.0.119 (Cloudflare proxy IP) |
| Cloudflare origin | 192.129.155.149 (user confirmed) |
| curl -I http://i.jueshi.net | 200 OK, Server: cloudflare |
| Nginx staging server block | ✅命中 (server_name i.jueshi.net → port 3001) |
| Deploy version via HTTP | v1.20.42.18.6.6.5.3-staging ✅ |

## 结论

DNS 已生效。Cloudflare 代理 i.jueshi.net → 旧服务器 192.129.155.149 → Nginx staging server block → PM2 xixiong-staging (port 3001)。

Staging deploy-version 确认通过 HTTPS 返回 `environment=staging`。
