# v1.20.42.18.6.1.1 — Phase 0: Schema 审计

**生成时间:** 2026-06-22

---

## 1. 独立论坛用户模型检查

| 模型名 | 是否存在 | 结果 |
|--------|---------|------|
| ForumUser | 否 | ✅ |
| CommunityUser | 否 | ✅ |
| ForumAccount | 否 | ✅ |
| ForumSession | 否 | ✅ |
| ForumAuth | 否 | ✅ |

**结论:** 无任何独立论坛用户模型。

## 2. 所有关系指向 User.id

| 模型 | 字段 | 指向 | 外键 |
|------|------|------|------|
| ForumPost | userId | User.id | `@relation(fields: [userId], references: [id], onDelete: Cascade)` ✅ |
| ForumComment | userId | User.id | `@relation(fields: [userId], references: [id], onDelete: Cascade)` ✅ |
| HonorLog | userId | User.id | `@relation(fields: [userId], references: [id], onDelete: Cascade)` ✅ |
| UserBadgeAward | userId | User.id | `@relation(fields: [userId], references: [id], onDelete: Cascade)` ✅ |
| CommunityStat | userId | User.id | `@relation(fields: [userId], references: [id], onDelete: Cascade)` ✅ |
| UserCommunityProfile | userId | User.id | `@relation(fields: [userId], references: [id], onDelete: Cascade)` ✅ |

**注意:** 字段名为 `userId`（非 `authorId`），但功能等价 — 所有论坛内容都关联到现有 User 表。

## 3. User 模型扩展字段

| 字段 | 类型 | 说明 |
|------|------|------|
| honorScore | Int @default(0) | v18.6.1 新增，荣誉值（不可消费）|
| levelKey | String? @default("lv1") | v1.20.19 已有，等级 key |
| growthValue | Int | v1.20.19 已有，成长值 |
| points | Int | 已有，积分（可消费）|
| badges | String[] | v1.21 已有，简单标识数组 |

**结论:** honorScore 是 User 表的扩展字段，不是独立表。

## 4. UserBadge 模型验证

UserBadge 是勋章**定义**表，不代表用户：
- 无 email 字段 ✅
- 无 password 字段 ✅
- 无 role 字段 ✅
- 仅有：key, name, description, iconText, color, category, conditionText, isActive, sortOrder

## 5. 现有认证模型（未修改）

- Account: NextAuth 账户表 ✅ 未修改
- Session: NextAuth 会话表 ✅ 未修改
- VerificationToken: NextAuth 验证令牌 ✅ 未修改
- PasswordResetToken: 密码重置令牌 ✅ 未修改

**判定:** PASS — 完全复用现有 User 系统，无独立论坛用户体系。
