# v1.20.42.18.6.4.6 — Failure and Recovery Plan

**Date:** 2026-06-22

## Failure Acknowledgment

v18.6.4.5 = FAILED USER VISUAL REVIEW.

### Postal Code Failures
- Toronto query returned Calgary/Edmonton — wrong results
- Root cause: `looksLikePostalCode("Toronto")` returned true because "TORONTO" matched `/^[A-Z0-9]{2,10}$/`
- This caused Toronto to be routed to postal code region search instead of city DB search
- Region search found postal codes starting with "T" (Alberta) → Calgary/Edmonton

### Community Failures
- PC layout changes not perceptible to user
- Still looks like narrow container list

### Self-evaluation error
- Should not have written "PASSED" without user confirmation
- This round: only READY_FOR_USER_VISUAL_REVIEW or FAILED

## Recovery Plan

1. Fix `looksLikePostalCode` — require at least one digit
2. Fix API city search — use full query instead of 3-char prefix
3. Fix misleading "51+ 国家邮编数据库" → "51+ 国家地址/邮编资料"
4. Create /community-preview-v2 (NOT modify existing /community)
5. Deploy and verify with 4 keyword tests
