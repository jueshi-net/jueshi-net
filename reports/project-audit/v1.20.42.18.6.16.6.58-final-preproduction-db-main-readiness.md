# v1.20.42.18.6.16.6.58 Pre-Production Database & Main Merge Readiness Report

**Date:** 2026-06-30  
**Status:** ✅ PREPROD_DB_AUDIT_CLEAR_MAIN_MERGE_READY  
**Mode:** AUDIT (read-only)

---

## Executive Summary

### ✅ 数据库审计结果：CLEAR

**结论：可以安全合并 staging 到 main，无需数据库迁移。**

- ✅ 无 schema.prisma 变化
- ✅ 无 prisma/migrations 变化
- ✅ 无 Prisma 包版本变化
- ✅ 新增 API 使用现有数据库模型
- ✅ 可以完全不触碰数据库发布

---

## PHASE 1 — Staging vs Main Commit Diff

### Commit 统计

```bash
git log main..staging --oneline | wc -l
```

**结果：** 约 120+ commits（从 v18.6.12 到 v18.6.16.6.57）

### 文件变更统计

```bash
git diff main..staging --stat | tail -1
```

**结果：**
```
471 files changed, 61263 insertions(+), 732 deletions(-)
```

### 主要变更类别

| 类别 | 文件数 | 说明 |
|------|--------|------|
| 报告文档 | ~200+ | project-audit 报告 |
| 审计工具 | ~100+ | jueshi-audit 工具和证据 |
| 功能代码 | ~50+ | Template Studio, Canvas Editor, API routes |
| 配置文件 | ~10 | next.config.ts, package.json |
| 其他 | ~100+ | 证据文件、测试文件等 |

---

## PHASE 2 — Changed Files List

### 核心功能变更文件

#### 1. SEO 配置（关键修复）

```diff
next.config.ts
- 移除 jueshi.net 和 www.jueshi.net 的 X-Robots-Tag: noindex, nofollow
+ 只对 i.jueshi.net (staging) 设置 noindex, nofollow
```

**影响：** 允许 Google 索引正式站

#### 2. Template Studio 新功能

```
src/app/(public)/tools/template-studio/
├── canvas-editor-full.tsx (新增，1976 行)
├── canvas-editor.tsx (新增)
├── canvas-print.css (新增)
├── canvas/[id]/edit/page.tsx (新增)
├── canvas/new/page.tsx (新增)
├── [id]/edit/page.tsx (新增)
├── new/page.tsx (新增)
└── page.tsx (修改)

src/lib/template-studio/
├── canvas-schema.ts (新增，372 行)
├── canvas-layout.ts (新增)
├── canvas-png-renderer.ts (新增)
├── canvas-safe-style.ts (新增)
├── template-schema.ts (新增，642 行)
├── seal-generator.tsx (新增)
└── pdf/ (新增目录，5 个文件)
```

**影响：** 新增 Template Studio 功能，使用 JSON 文件存储，不使用数据库

#### 3. API Routes（新增）

```
src/app/api/
├── auth/me/route.ts (新增)
├── forum/related/route.ts (新增)
├── template-studio/templates/
│   ├── route.ts (新增，使用 JSON 文件存储)
│   └── [id]/route.ts (新增)
└── workspace/
    ├── document-drafts/
    │   ├── route.ts (新增，使用现有 Prisma 模型)
    │   └── [id]/route.ts (新增)
    └── products/
        ├── route.ts (新增，使用现有 Prisma 模型)
        └── [id]/route.ts (新增)
```

**影响：** 
- template-studio/templates 使用 JSON 文件存储，不使用数据库
- workspace/document-drafts 和 products 使用现有 Prisma 模型，无 schema 变化

#### 4. 页面和组件

```
src/app/(public)/
├── bbs/[slug]/page.tsx (修改)
├── bbs/new/page.tsx (修改)
├── resources-v2/ (新增，7 个文件)
└── tools/ (多个文件修改)

src/app/(workspace)/
├── workspace/page.tsx (修改)
├── workspace/products/page.tsx (修改)
└── workspace/templates/page.tsx (修改)

src/components/
├── bbs/post-form.tsx (修改)
├── document-runtime/ (新增，4 个文件)
├── document-tools/ (新增，3 个文件)
└── product-selector.tsx (修改)

src/lib/
├── document-runtime/ (新增，4 个文件)
├── task-chain.ts (修改)
└── tool-content/tool-content-registry.ts (新增)
```

**影响：** 新增功能和页面，使用现有数据库结构

---

## PHASE 3 — Schema.prisma 变化检查

### 检查结果

```bash
git diff main..staging prisma/schema.prisma
```

**结果：** 空（无变化）

```bash
git diff main..staging --name-only | grep -E "schema\.prisma"
```

**结果：** 无匹配

### 结论

✅ **schema.prisma 完全未变化**

- 无新表
- 无新字段
- 无字段类型变化
- 无索引变化
- 无关系变化

---

## PHASE 4 — Prisma/Migrations 变化检查

### 检查结果

```bash
git diff main..staging --name-only | grep -i migration
```

**结果：** 无匹配

```bash
git diff main..staging prisma/migrations/
```

**结果：** 空（无变化）

### 结论

✅ **无 migration 文件变化**

- 无新 migration
- 无 migration 修改
- 无 migration 删除

---

## PHASE 5 — Package Prisma 变化检查

### 检查结果

```bash
git diff main..staging package.json | grep -E "prisma|@prisma"
```

**结果：** 无 Prisma 版本变化

```bash
git diff main..staging package.json package-lock.json | grep -E "^\+.*prisma|^\-.*prisma"
```

**结果：** 无匹配

### package.json 实际变化

```diff
+ "html-to-image": "^1.11.13"
+ "@types/pngjs": "^6.0.5"
+ "pngjs": "^7.0.0"
```

**新增的依赖：**
- `html-to-image`: 用于 Canvas PNG 导出（data-driven renderer）
- `@types/pngjs` 和 `pngjs`: 用于 PNG 像素分析

**结论：**
✅ **无 Prisma 包版本变化**

---

## PHASE 6 — DB Runtime Compatibility Risk

### 风险评估

| 风险项 | 级别 | 说明 |
|--------|------|------|
| Schema 不兼容 | ✅ 无 | schema.prisma 未变化 |
| Migration 缺失 | ✅ 无 | 无新 migration |
| Prisma 版本不兼容 | ✅ 无 | Prisma 版本未变化 |
| API 使用新模型 | ✅ 无 | 所有 API 使用现有模型 |
| 数据格式变化 | ✅ 无 | 无数据格式变化 |

### API 路由数据库使用分析

#### 1. template-studio/templates

```typescript
// STORAGE: JSON file (PARTIAL_STORAGE) — no Prisma model yet.
// File: process.cwd()/data/template-studio-templates.json
```

**数据库使用：** 无（使用 JSON 文件存储）

**风险：** ✅ 无

#### 2. workspace/document-drafts

```typescript
import { prisma } from "@/lib/prisma";

const drafts = await prisma.toolDocumentDraft.findMany({
  where: { userId: session.user.id },
  // ...
});
```

**数据库使用：** 使用现有 `toolDocumentDraft` 模型

**风险：** ✅ 无（模型已存在于 main）

#### 3. workspace/products

```typescript
import { prisma } from "@/lib/prisma";

const products = await prisma.productItem.findMany({
  where: { userId: session.user.id },
  // ...
});
```

**数据库使用：** 使用现有 `productItem` 模型

**风险：** ✅ 无（模型已存在于 main）

### 结论

✅ **DB Runtime Compatibility Risk: 无**

- 所有新增 API 使用现有数据库模型
- 无新表、无新字段
- 无数据格式变化
- 可以安全部署到 production，无需数据库迁移

---

## PHASE 7 — 是否可以不触碰 DB 发布

### 分析

1. **schema.prisma 未变化** → 无需 `prisma db push` 或 `prisma migrate deploy`
2. **无 migration 文件** → 无需执行 migration
3. **Prisma 版本未变化** → 无需更新 Prisma client
4. **API 使用现有模型** → 无需数据库结构变化

### 结论

✅ **可以完全不触碰数据库发布**

**Production 部署步骤（无需 DB 操作）：**

```bash
# 1. SSH 到 production server
ssh deploy@104.250.109.99

# 2. 进入项目目录
cd /home/deploy/xixiong-saas

# 3. 拉取最新代码
git pull origin main

# 4. 安装依赖（如果有新依赖）
npm ci

# 5. 构建应用
npm run build

# 6. 重启应用
pm2 restart xixiong-saas

# 7. 验证部署
curl -sI https://jueshi.net/ | grep -i x-robots
# 预期: 无输出（noindex 已移除）
```

**禁止执行的命令：**

```bash
# ❌ 禁止执行
prisma db push
prisma migrate deploy
prisma migrate reset
prisma db seed
```

---

## PHASE 8 — 是否 Main 已包含 Staging 修复

### 检查结果

```bash
git log main --oneline -1
```

**结果：**
```
e0251fb ops: v18.6.12 production release current verified build
```

```bash
git log staging --oneline -1
```

**结果：**
```
9f014c6 docs: v18.6.16.6.57 production release SEO report
```

### 关键修复对比

| 修复 | Main | Staging |
|------|------|---------|
| SEO noindex 修复 | ❌ 无 | ✅ a7bcf6a |
| Data-driven PNG renderer | ❌ 无 | ✅ 96dd9a7 |
| Canvas baseline restore | ❌ 无 | ✅ c8760b6 |
| Template Studio 功能 | ❌ 无 | ✅ 多个 commit |
| Output boundary 修复 | ❌ 无 | ✅ c18824f |

### 结论

❌ **Main 未包含 Staging 修复**

- Main 停留在 v18.6.12
- Staging 已到 v18.6.16.6.57
- 需要合并 staging 到 main

---

## PHASE 9 — 正确 Production 手动部署顺序

### 推荐部署顺序

#### Step 1: 合并 Staging 到 Main

```bash
# 本地操作
cd ~/xixiong-saas

# 切换到 main
git checkout main

# 合并 staging
git merge staging

# 推送到远程
git push origin main
```

#### Step 2: Production 部署

```bash
# SSH 到 production server
ssh deploy@104.250.109.99

# 进入项目目录
cd /home/deploy/xixiong-saas

# 记录当前 commit（用于回滚）
git log --oneline -1 > /tmp/production-backup-commit.txt

# 拉取最新代码
git pull origin main

# 安装依赖（如果有新依赖）
npm ci

# 构建应用
npm run build

# 重启应用
pm2 restart xixiong-saas
```

#### Step 3: Production 验证

```bash
# 验证 SEO 修复
curl -sI https://jueshi.net/ | grep -i x-robots
# 预期: 无输出

# 验证首页
curl -sI https://jueshi.net/ | head -1
# 预期: HTTP/2 200

# 验证 Template Studio
curl -sI https://jueshi.net/tools/template-studio | head -1
# 预期: HTTP/2 200

# 验证 robots.txt
curl -s https://jueshi.net/robots.txt | head -5
# 预期: Allow: /

# 验证 sitemap
curl -s https://jueshi.net/sitemap.xml | head -10
# 预期: URL 为 jueshi.net
```

#### Step 4: 回滚计划（如果需要）

```bash
# SSH 到 production server
ssh deploy@104.250.109.99

# 进入项目目录
cd /home/deploy/xixiong-saas

# 查看备份的 commit
cat /tmp/production-backup-commit.txt

# 回滚到之前的 commit
git reset --hard <commit-hash>

# 重新构建
npm run build

# 重启
pm2 restart xixiong-saas
```

---

## PHASE 10 — 需要用户确认的下一步

### 立即行动

1. **确认合并 staging 到 main：**
   ```bash
   git checkout main
   git merge staging
   git push origin main
   ```

2. **确认 production 部署：**
   ```bash
   ssh deploy@104.250.109.99
   cd /home/deploy/xixiong-saas
   git pull origin main
   npm ci
   npm run build
   pm2 restart xixiong-saas
   ```

3. **确认 production 验证：**
   ```bash
   curl -sI https://jueshi.net/ | grep -i x-robots
   # 预期: 无输出
   ```

### 后续行动

1. **添加 Google Search Console：**
   - 验证域名
   - 提交 sitemap
   - 请求索引

2. **配置 Cloudflare 重定向（可选）：**
   - www → non-www
   - 300zy.com → jueshi.net

---

## PHASE 11 — 禁止执行的命令清单

### ❌ 绝对禁止

```bash
# 数据库操作（无需执行）
prisma db push
prisma migrate deploy
prisma migrate reset
prisma db seed
prisma migrate dev

# 破坏性操作
git reset --hard origin/main  # 除非回滚
git push --force
DROP TABLE
DELETE FROM
TRUNCATE TABLE

# Production 直接修改
ssh deploy@104.250.109.99 "cd /home/deploy/xixiong-saas && git checkout staging"
ssh deploy@104.250.109.99 "cd /home/deploy/xixiong-saas && git merge staging"
```

### ✅ 允许执行

```bash
# 本地操作
git checkout main
git merge staging
git push origin main

# Production 部署（标准流程）
ssh deploy@104.250.109.99
cd /home/deploy/xixiong-saas
git pull origin main
npm ci
npm run build
pm2 restart xixiong-saas

# 验证
curl -sI https://jueshi.net/ | grep -i x-robots
curl -s https://jueshi.net/robots.txt
curl -s https://jueshi.net/sitemap.xml
```

---

## PHASE 12 — 安全确认

| 检查项 | 状态 |
|--------|------|
| 是否触碰 Template Studio | ✅ 否（只是合并已有代码） |
| 是否修改 canvas-editor-full.tsx | ✅ 否（只是合并已有代码） |
| 是否修改打印/PNG/PDF | ✅ 否（只是合并已有代码） |
| 是否触碰 production 数据库 | ✅ 否（无需 DB 操作） |
| 是否执行 prisma db push | ✅ 否（禁止） |
| 是否执行 migration | ✅ 否（禁止） |
| 是否 destructive SQL | ✅ 否（禁止） |
| 是否输出 secret | ✅ 否 |
| 是否自动部署 production | ✅ 否（需要用户手动） |

---

## PHASE 13 — DB 安全结果

### Production DB

**未执行任何 DB 操作：**
- ✅ 未执行 prisma db push
- ✅ 未执行 migration
- ✅ 未执行 destructive SQL
- ✅ 9833416@qq.com 未修改

### Staging DB

**未执行任何 DB 操作：**
- ✅ 未执行 prisma db push
- ✅ 未执行 migration
- ✅ 未执行 destructive SQL

---

## 最终判定

### ✅ PREPROD_DB_AUDIT_CLEAR_MAIN_MERGE_READY

**审计结果：**

- ✅ schema.prisma 无变化
- ✅ prisma/migrations 无变化
- ✅ Prisma 包版本无变化
- ✅ DB runtime compatibility risk: 无
- ✅ 可以完全不触碰数据库发布
- ❌ main 未包含 staging 修复（需要合并）

**下一步：**

1. 用户确认合并 staging 到 main
2. 用户确认 production 部署
3. 用户确认 production 验证

---

## 附件

- `reports/project-audit/v1.20.42.18.6.16.6.56-01-seo-current-indexing-audit.md` — SEO 诊断报告
- `reports/project-audit/v1.20.42.18.6.16.6.56-final-seo-indexing-cleanup-plan.md` — SEO 修复方案
- `reports/project-audit/v1.20.42.18.6.16.6.57-final-production-release-seo-report.md` — Production 部署报告
- `reports/project-audit/v1.20.42.18.6.16.6.58-final-preproduction-db-main-readiness.md` — 本报告

---

**Report generated:** 2026-06-30  
**Report version:** v1.20.42.18.6.16.6.58  
**Status:** PREPROD_DB_AUDIT_CLEAR_MAIN_MERGE_READY
