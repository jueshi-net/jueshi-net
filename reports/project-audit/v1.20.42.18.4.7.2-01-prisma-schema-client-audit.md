# v1.20.42.18.4.7.2 — Prisma Schema / Client 一致性审计

**生成时间:** 2026-06-21

---

## 1. Schema 审计

### 本地 schema.prisma

| Model | 位置 | @@map |
|-------|------|-------|
| Guide | line 1729 | "guides" |
| Checklist | line 1760 | "checklists" |

### 生产 schema.prisma (VPS)

| Model | 位置 | @@map |
|-------|------|-------|
| Guide | line 1729 | "guides" |
| Checklist | line 1760 | "checklists" |

### 一致性

**本地和生产 schema 完全一致。** 两个 model 都存在，名称为 `Checklist`（大写 C），`@@map("checklists")`。

## 2. Model 名称确认

- Schema 中的 model 名称: `Checklist`（大写 C）
- Prisma Client 访问器: `prisma.checklist`（Prisma 自动首字母小写）
- 代码中使用: `prisma.checklist.findUnique(...)` ✅ 正确
- 代码中使用: `prisma.guide.findMany(...)` ✅ 正确

## 3. Prisma Client 生成状态

### 本地

```
npx prisma generate → ✔ Generated Prisma Client (v7.8.0)
```

验证:
- `get checklist(): Prisma.ChecklistDelegate<ExtArgs, ClientOptions>` ✅ 存在
- `get guide(): Prisma.GuideDelegate<ExtArgs, ClientOptions>` ✅ 存在
- `.prisma/client/index.d.ts` 包含 29 处 "checklist" 匹配
- `.prisma/client/index.d.ts` 包含 52 处 "guide" 匹配

### 生产 (VPS)

```
npx prisma generate → ✔ Generated Prisma Client (v7.8.0)
```

验证:
- `get checklist(): Prisma.ChecklistDelegate<ExtArgs, ClientOptions>` ✅ 存在
- `get guide(): Prisma.GuideDelegate<ExtArgs, ClientOptions>` ✅ 存在

## 4. TypeScript 编译检查

```
npx tsc --noEmit
```

- **无 checklist/guide 相关类型错误** ✅
- **无 PrismaClient 相关类型错误** ✅
- 仅有 tests/ 目录下的 pre-existing 错误（与 v18.4.7 无关）

## 5. @prisma/client 导出链

```
@prisma/client/index.d.ts → export * from '.prisma/client/default'
@prisma/client/index.js → module.exports = { ...require('.prisma/client/default') }
```

导出链正确，`.prisma/client/default` 包含 Guide 和 Checklist 类型。

## 6. 结论

**Schema、Prisma Client、代码三者完全一致。** 之前的 `Property 'checklist' does not exist on type 'PrismaClient'` 错误是因为 Prisma Client 未在 schema 更新后重新生成。执行 `npx prisma generate` 后问题解决。
