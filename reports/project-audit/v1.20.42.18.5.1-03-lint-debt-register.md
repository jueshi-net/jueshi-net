# v1.20.42.18.5.1 — Phase 3: lint 技术债归档

**生成时间:** 2026-06-22

---

## 1. 当前 lint 问题数量

| 类型 | 数量 |
|------|------|
| Total problems | 1781 |
| Errors | 1200 |
| Warnings | 581 |
| Fixable with --fix | 10 errors + 1 warning |

## 2. 是否为本轮新增

**❌ 否。** 全部为历史遗留。本轮新增的 `task-chain-cta.tsx` 组件和 4 个页面的 import 无新增 lint 错误。

## 3. 是否影响 build

**❌ 否。** `npm run build` 在 VPS 上通过 (exit 0)。lint 问题不阻断 Next.js build。

## 4. 是否影响 Beta P0/P1

**❌ 否。** lint 问题集中在测试脚本和旧验证文件中，不影响生产页面渲染、API 权限、用户流程。

## 5. 按类型分类

| 规则 | 数量 | 来源 | 影响 |
|------|------|------|------|
| `@typescript-eslint/no-require-imports` | ~15 | `.cjs`/`.js` 旧测试脚本 (check-admin.js, verify-fix.cjs, v1.20.34.4-test.cjs 等) | 无 — 测试脚本不参与 build |
| `@typescript-eslint/no-explicit-any` | ~800+ | src/ 各组件中 `as any` 类型断言 | 低 — 不影响运行时 |
| `@typescript-eslint/no-unused-vars` | ~300+ | 各文件中未使用变量 | 低 — 不影响运行时 |
| `prefer-const` | 少量 | 测试脚本 | 无 |
| 其他 (formatting/accessibility/import order) | ~400+ | 各文件 | 低 |

## 6. 建议

**建议未来单独开 v18.6 lint debt sprint。** 本轮不大规模格式化全仓库。

优先级：
1. 将 `.cjs`/`.js` 旧测试脚本移到 `scripts/legacy/` 并从 lint 排除
2. 逐步清理 `no-explicit-any` — 按 API 层 → 组件层 → 页面层顺序
3. 清理 `no-unused-vars` — 大部分是简单删除

## 7. 本轮处理

**不处理历史 lint。** 只归档。本轮新增代码无 lint 问题。

---

## 判定: lint 历史问题归档为技术债，不混入 Beta 阻断项 ✅
