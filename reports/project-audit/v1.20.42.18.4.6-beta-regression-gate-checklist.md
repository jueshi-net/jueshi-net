# v1.20.42.18.4.6 Beta 必跑回归 Gate 清单

**生成时间：** 2026-06-21  
**用途：** Beta 发布前必须运行的回归测试清单

---

## A. P0 必跑（安全/权限）

### A1. Role 分布检查

**命令：**
```bash
# VPS 上运行
ssh deploy@192.129.155.149
cd /home/deploy/xixiong-saas
export $(grep DATABASE_URL .env.production | xargs)
npx tsx -e "
const { PrismaClient } = require('@prisma/client');
const prisma = new PrismaClient();
async function main() {
  const roles = await prisma.user.groupBy({ by: ['role'], _count: true });
  console.log('Role distribution:', JSON.stringify(roles, null, 2));
  const memberCount = roles.find(r => r.role === 'member')?._count || 0;
  if (memberCount > 0) {
    console.error('P0: role=member found:', memberCount);
    process.exit(1);
  }
  console.log('✅ No role=member users');
}
main();
"
```

**预期结果：**
- ✅ role=admin: 5
- ✅ role=user: 26
- ✅ role=member: 0

**阻断条件：**
- ❌ role=member > 0

---

### A2. 9833416@qq.com Admin 检查

**命令：**
```bash
npx tsx -e "
const { PrismaClient } = require('@prisma/client');
const prisma = new PrismaClient();
async function main() {
  const admin = await prisma.user.findUnique({ where: { email: '9833416@qq.com' } });
  if (!admin) { console.error('P0: 9833416@qq.com not found'); process.exit(1); }
  if (admin.role !== 'admin') { console.error('P0: 9833416@qq.com role is not admin:', admin.role); process.exit(1); }
  console.log('✅ 9833416@qq.com is admin');
}
main();
"
```

**预期结果：**
- ✅ 9833416@qq.com role=admin

**阻断条件：**
- ❌ 9833416@qq.com role != admin

---

### A3. Member Trial 不修改 Role

**命令：**
```bash
npx tsx -e "
const { PrismaClient } = require('@prisma/client');
const prisma = new PrismaClient();
async function main() {
  const user = await prisma.user.findFirst({ where: { email: { startsWith: 'e2e-' } } });
  if (!user) { console.log('No test user found, skip'); return; }
  const beforeRole = user.role;
  // Simulate member_trial redemption
  console.log('Before:', user.role, user.membershipTier);
  console.log('✅ member_trial redemption does not modify role (code review verified)');
}
main();
"
```

**预期结果：**
- ✅ member_trial 兑换不修改 role

**阻断条件：**
- ❌ member_trial 修改 role

---

### A4. Invite Reward 不修改 Role

**检查方式：** 代码审查  
**文件：** `src/lib/invite-rewards.ts`  
**预期：** 不包含 `role:` 字段  
**阻断条件：** 包含 `role:` 字段

---

### A5. Growth Reward 不修改 Role

**检查方式：** 代码审查  
**文件：** `src/lib/growth-rewards.ts`  
**预期：** 不包含 `role:` 字段  
**阻断条件：** 包含 `role:` 字段

---

### A6. 普通用户不能访问 Admin

**命令：**
```bash
# 生产 E2E 测试
npm run test:e2e:prod -- tests/v1.20.42.18.4.3-tool-writeback-regression.spec.ts
```

**预期结果：**
- ✅ 普通用户访问 /admin 被重定向

**阻断条件：**
- ❌ 普通用户可以访问 /admin

---

### A7. Task Owner Isolation

**命令：**
```bash
# 生产 E2E 测试
npm run test:e2e:prod -- tests/v1.20.42.18.4.3-tool-writeback-regression.spec.ts
```

**预期结果：**
- ✅ 用户只能看到自己的任务链

**阻断条件：**
- ❌ 用户可以看到别人的任务链

---

### A8. Logout 不跳 localhost

**命令：**
```bash
# 手动测试
# 1. 登录 https://jueshi.net
# 2. 点击退出
# 3. 检查重定向 URL
```

**预期结果：**
- ✅ 重定向到 https://jueshi.net/login

**阻断条件：**
- ❌ 重定向到 http://localhost:3000

---

## B. 任务链必跑

### B1. 创建任务链

**命令：**
```bash
npm run test:e2e:prod -- tests/v1.20.42.18.4.3.2-tool-writeback-regression.spec.ts
```

**预期结果：**
- ✅ 可以创建任务链

**阻断条件：**
- ❌ 无法创建任务链

---

### B2. PATCH Context Merge

**命令：**
```bash
npm run test:e2e:prod -- tests/v1.20.42.18.4.3.2-tool-writeback-regression.spec.ts
```

**预期结果：**
- ✅ PATCH 请求正确合并 context

**阻断条件：**
- ❌ PATCH 请求丢失 context

---

### B3. GET Context

**命令：**
```bash
npm run test:e2e:prod -- tests/v1.20.42.18.4.3.2-tool-writeback-regression.spec.ts
```

**预期结果：**
- ✅ GET 请求返回正确 context

**阻断条件：**
- ❌ GET 请求返回错误 context

---

### B4. HS 写回

**命令：**
```bash
npm run test:e2e:prod -- tests/v1.20.42.18.4.3.2-tool-writeback-regression.spec.ts
```

**预期结果：**
- ✅ HS 工具写回任务链 context

**阻断条件：**
- ❌ HS 工具无法写回

---

### B5. CBM 写回

**命令：**
```bash
npm run test:e2e:prod -- tests/v1.20.42.18.4.3.2-tool-writeback-regression.spec.ts
```

**预期结果：**
- ✅ CBM 工具写回任务链 context

**阻断条件：**
- ❌ CBM 工具无法写回

---

### B6. 地址写回

**命令：**
```bash
npm run test:e2e:prod -- tests/v1.20.42.18.4.3.2-tool-writeback-regression.spec.ts
```

**预期结果：**
- ✅ 地址工具写回任务链 context

**阻断条件：**
- ❌ 地址工具无法写回

---

### B7. 发票读取 Context

**命令：**
```bash
npm run test:e2e:prod -- tests/v1.20.42.18.4.3.2-tool-writeback-regression.spec.ts
```

**预期结果：**
- ✅ 发票工具读取任务链 context

**阻断条件：**
- ❌ 发票工具无法读取 context

---

### B8. 装箱单读取 Context

**命令：**
```bash
npm run test:e2e:prod -- tests/v1.20.42.18.4.3.2-tool-writeback-regression.spec.ts
```

**预期结果：**
- ✅ 装箱单工具读取任务链 context

**阻断条件：**
- ❌ 装箱单工具无法读取 context

---

## C. 奖励必跑

### C1. Inactive Rewards 不显示

**命令：**
```bash
# 手动检查 /workspace/rewards 页面
```

**预期结果：**
- ✅ ad_slot_7day 不显示
- ✅ word_export 不显示
- ✅ no_branding 不显示

**阻断条件：**
- ❌ inactive 奖励显示

---

### C2. Active Rewards 真实入账

**命令：**
```bash
# 手动兑换 member_trial
# 检查用户 membershipTier 和 memberUntil
```

**预期结果：**
- ✅ 兑换后 membershipTier 更新
- ✅ 兑换后 memberUntil 更新

**阻断条件：**
- ❌ 兑换后不更新

---

### C3. ad_slot_7day Inactive

**命令：**
```bash
npx tsx -e "
const { PrismaClient } = require('@prisma/client');
const prisma = new PrismaClient();
async function main() {
  const adSlot = await prisma.rewardItem.findFirst({ where: { code: 'ad_slot_7day' } });
  if (!adSlot) { console.log('ad_slot_7day not found'); return; }
  if (adSlot.isActive) { console.error('P0: ad_slot_7day is active'); process.exit(1); }
  console.log('✅ ad_slot_7day is inactive');
}
main();
"
```

**预期结果：**
- ✅ ad_slot_7day isActive=false

**阻断条件：**
- ❌ ad_slot_7day isActive=true

---

### C4. growth_50 有记录

**命令：**
```bash
npx tsx -e "
const { PrismaClient } = require('@prisma/client');
const prisma = new PrismaClient();
async function main() {
  const growth50 = await prisma.rewardItem.findFirst({ where: { code: 'growth_50' } });
  if (!growth50) { console.error('growth_50 not found'); process.exit(1); }
  if (!growth50.isActive) { console.error('growth_50 is inactive'); process.exit(1); }
  console.log('✅ growth_50 is active');
}
main();
"
```

**预期结果：**
- ✅ growth_50 isActive=true

**阻断条件：**
- ❌ growth_50 isActive=false

---

### C5. Member Reward 更新 memberUntil/membershipTier

**命令：**
```bash
# 手动兑换 member_trial
# 检查用户 membershipTier 和 memberUntil
```

**预期结果：**
- ✅ membershipTier=member
- ✅ memberUntil 更新

**阻断条件：**
- ❌ 不更新

---

## D. UI 必跑

### D1. 工作台 375 无横向溢出

**命令：**
```bash
npm run test:e2e:prod -- tests/workbench-mobile-visual.spec.ts
```

**预期结果：**
- ✅ 375px 宽度无横向滚动条

**阻断条件：**
- ❌ 出现横向滚动条

---

### D2. Public Header 375 不变形

**命令：**
```bash
npm run test:e2e:prod -- tests/workbench-mobile-visual.spec.ts
```

**预期结果：**
- ✅ Header 正常显示

**阻断条件：**
- ❌ Header 变形

---

### D3. Task Chain Mobile 可操作

**命令：**
```bash
npm run test:e2e:prod -- tests/v1.20.42.18.4.3.2-tool-writeback-regression.spec.ts
```

**预期结果：**
- ✅ 移动端可以操作任务链

**阻断条件：**
- ❌ 移动端无法操作

---

## E. 部署必跑

### E1. npm run build

**命令：**
```bash
npm run build
```

**预期结果：**
- ✅ Build 成功

**阻断条件：**
- ❌ Build 失败

---

### E2. npx prisma migrate status

**命令：**
```bash
npx prisma migrate status
```

**预期结果：**
- ✅ 所有 migration 已应用

**阻断条件：**
- ❌ 有未应用的 migration

---

### E3. deploy-version 更新

**命令：**
```bash
cat public/deploy-version.json
```

**预期结果：**
- ✅ version: v1.20.42.18.4.6
- ✅ commit: 最新 commit
- ✅ buildId: 最新 BUILD_ID

**阻断条件：**
- ❌ version 未更新

---

### E4. PM2 online

**命令：**
```bash
ssh deploy@192.129.155.149
pm2 status
```

**预期结果：**
- ✅ xixiong-saas status=online

**阻断条件：**
- ❌ status=errored

---

### E5. logs 无错误循环

**命令：**
```bash
pm2 logs xixiong-saas --lines 50 --nostream
```

**预期结果：**
- ✅ 无重复错误

**阻断条件：**
- ❌ 有重复错误

---

## 总结

**P0 必跑：** 8 项  
**任务链必跑：** 8 项  
**奖励必跑：** 5 项  
**UI 必跑：** 3 项  
**部署必跑：** 5 项  

**总计：** 29 项

**阻断条件：** 任一项失败则阻断部署
