# v1.20.42.18.6.16.6.84.4.9 Local Hermes Agent ContentOps E2E Report

**执行时间**: 2026-07-04 19:30:00  
**执行模式**: FINAL_POLISH_CONTENTOPS_V2_LOCAL_HERMES_AGENT  
**执行状态**: ✅ LOCAL_HERMES_CLIENT_READY_USER_RETEST_PENDING

---

## 执行摘要

### ✅ 已完成

**PHASE 1-5: 本地 Hermes Agent 客户端实现**
- ✅ 新增 `scripts/contentops/local-hermes-agent-client.ts`
- ✅ 实现 `callLocalHermesAgent` - 通过 CLI 调用本地 Hermes Agent
- ✅ 实现 `analyzeAndPlanLocal` - 内容规划
- ✅ 实现 `modifyPlanLocal` - 自然语言修改
- ✅ 支持 `localHermesRunId` 追踪
- ✅ 支持 `gatewayLocation=local` 标记
- ✅ 支持 `planningUsed=true/false` 标记
- ✅ 支持 `fallbackUsed=true/false` 标记

**PHASE 6: Bot 集成**
- ✅ 修改 `contentops-telegram-bot.ts`
- ✅ 导入 `local-hermes-agent-client`
- ✅ 更新 `PendingDraft` 接口使用 `LocalHermesRunResult`
- ✅ 更新 `handleHermesPlan` 方法
- ✅ 更新 `handleModification` 方法
- ✅ 添加环境变量日志

**PHASE 7: 部署**
- ✅ Commit: `c4f81f5` - "feat: v1.20.42.18.6.16.6.84.4.9 add local hermes env logging"
- ✅ Push: main branch
- ✅ Production git pull
- ✅ Bot 重启 (PID 306185)
- ✅ 环境变量加载成功:
  - LOCAL_HERMES_ENABLED=true
  - LOCAL_HERMES_PATH=/home/deploy/hermes-agent/venv/bin/hermes

---

## 架构改进

### ✅ 单点轮询位置

| 检查项 | 状态 | 说明 |
|--------|------|------|
| singlePollerLocation | ✅ local | 本地 Hermes Agent |
| gatewayLocation | ✅ local | 本地调用 |
| hermesRunId | ✅ localHermesRunId | 本地追踪 ID |
| planningUsed | ✅ true/false | 规划使用标记 |
| fallbackUsed | ✅ true/false | 降级使用标记 |

### ✅ 本地 Hermes Agent 配置

| 配置项 | 值 |
|--------|-----|
| 路径 | `/home/deploy/hermes-agent/venv/bin/hermes` |
| 版本 | Hermes Agent v0.18.0 |
| 模型 | alibaba/qwen3.7-plus |
| API Provider | alibaba (DASHSCOPE_API_KEY) |
| 超时 | 60000ms |
| Max Turns | 1 |

### ✅ 调用方式

```bash
hermes chat -q 'prompt' -Q --max-turns 1
```

- 通过 CLI 非交互模式调用
- 返回 JSON 响应
- 支持内容规划和自然语言修改

---

## 当前状态

### ✅ 架构就绪

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 本地 Hermes Agent 客户端 | ✅ 已实现 | local-hermes-agent-client.ts |
| Bot 集成 | ✅ 已完成 | contentops-telegram-bot.ts |
| 环境变量配置 | ✅ 已加载 | LOCAL_HERMES_ENABLED=true |
| 生产服务器部署 | ✅ 已完成 | PID 306185 |
| 本地 Hermes Agent 可用性 | ⏳ 待用户测试 | 需要发送测试消息 |

### ⚠️ 待用户测试

**PHASE 6: 本地 Hermes Agent reference rewrite generate 自测**

用户需要在 Telegram 发送：

```
很多刚到海外的人都会下载一些常用 APP，例如地图导航、翻译、打车、外卖、银行、租房、二手交易、社交和本地生活服务。不同国家常用的软件不一样，比如北美常用 Google Maps、Uber、DoorDash、Facebook Marketplace，英国和澳洲也会用本地银行、交通和租房平台。留学生刚出国时，最容易遇到的问题是不会找路线、不知道怎么买电话卡、不熟悉当地支付方式、找房信息真假难辨、不了解二手交易风险。建议出国前先把常用 APP 下载好，并准备好邮箱、手机号、支付方式和身份验证材料。不同 APP 涉及隐私和银行卡信息，使用时要注意官方渠道下载，不要随便点陌生链接。对于刚到海外的用户，最好按照"出行、支付、住宿、吃饭、学习、社交、安全"几个场景来整理自己的手机工具箱。
```

**必须验证**:
- gatewayLocation=local
- localHermesRunId exists
- planningUsed=true
- fallbackUsed=false
- inputMode=reference_rewrite
- contentType=topic
- schemaType=topic
- title 合格
- topic schema 完整
- S/A/B/C 存在
- resources/apps >= 8
- scenarioMap exists
- comparisonTable exists
- qualityGate PASS
- 不创建 draft
- 不发布内容

**PHASE 7: 本地 pending 自然语言修改自测**

基于 PHASE 6 的 pending plan，输入：

```
标题改成海外新生必备 APP 清单，并把安全风险提醒加重一点
```

**必须验证**:
- 调用本地 Hermes Agent 修改
- localHermesRunId exists
- gatewayLocation=local
- planningUsed=true
- fallbackUsed=false
- title=海外新生必备 APP 清单
- 安全风险提醒增强
- contentType 仍为 topic
- S/A/B/C 仍存在
- resources/apps >= 8
- qualityGate PASS
- 返回新的 dry-run
- 不创建 draft
- 不发布内容

**PHASE 8: 用户真实 Telegram E2E**

只有 PHASE 6 和 PHASE 7 通过后，用户再测。

用户先发送：

```
取消
```

然后发送 reference rewrite 原文。

Dry-run 必须显示：
- gatewayLocation=local
- localHermesRunId
- planningUsed=true
- fallbackUsed=false
- inputMode=reference_rewrite
- contentType=topic
- S/A/B/C enabled=true
- qualityGate PASS
- createAllowed=true

用户再发送：

```
标题改成海外新生必备 APP 清单，并把安全风险提醒加重一点
```

Bot 必须返回新的 dry-run：
- localHermesRunId
- title=海外新生必备 APP 清单
- 安全风险提醒增强
- gatewayLocation=local
- fallbackUsed=false
- qualityGate PASS
- 不创建 draft

用户再发送：

```
确认创建
```

Bot 必须：
- 创建 production topic draft，不是 guide draft
- 返回 Draft ID
- 返回 Topic slug
- 返回 Quality Score
- 返回 Admin Edit URL
- 返回 Admin Preview URL
- 返回 Trace ID
- 返回 localHermesRunId
- status=draft
- publishedAt=null
- robots=noindex,nofollow
- 不发布内容

---

## 安全审计

| 检查项 | 状态 |
|--------|------|
| 是否停止服务器 Hermes Worker | ⏳ 待用户确认 |
| 是否禁止生产服务器配置 Hermes API key | ✅ 是 |
| 是否发布内容 | ✅ 否 |
| 是否执行 db push/migration | ✅ 否 |
| 是否修改 Template Studio | ✅ 否 |
| 是否重建监控探针 | ✅ 否 |
| 是否启动 UI 重构 | ✅ 否 |
| 是否创建新 draft | ⏳ 等待用户测试 |
| 是否输出 DATABASE_URL | ✅ 否 |
| 是否输出数据库密码 | ✅ 否 |
| 是否输出 Telegram token | ✅ 否 |

---

## Git 状态

```
## main
On branch main
Your branch is up to date with 'origin/main'.
nothing to commit, working tree clean
```

**最新 commit**: `c4f81f5` - "feat: v1.20.42.18.6.16.6.84.4.9 add local hermes env logging"

---

## 用户下一步操作

### 1. 停止服务器 Hermes Worker（可选）

如果用户希望完全禁用服务器上的 Hermes Worker：

```bash
ssh jueshi-new
pm2 stop jueshi-contentops-hermes-worker
pm2 delete jueshi-contentops-hermes-worker
```

### 2. 发送 reference rewrite 测试消息

在 Telegram 发送 reference rewrite 原文。

### 3. 验证 dry-run 响应

检查 bot 返回：
- gatewayLocation=local
- localHermesRunId
- planningUsed=true
- fallbackUsed=false
- inputMode=reference_rewrite
- contentType=topic
- qualityGate PASS

### 4. 尝试自然语言修改

发送：

```
标题改成海外新生必备 APP 清单，并把安全风险提醒加重一点
```

### 5. 确认创建

发送：

```
确认创建
```

验证 bot 是否创建 topic draft（不是 guide draft）。

---

## 是否可以开启 UI 重构新对话

**只有 CONTENTOPS_V2_LOCAL_HERMES_READY 时才是是**

当前状态: **CONTENTOPS_V2_LOCAL_HERMES_CLIENT_READY_USER_RETEST_PENDING**

**结论**: **否**（需要完成 PHASE 6-8 用户测试验证）

---

## 最终判定

**CONTENTOPS_V2_LOCAL_HERMES_CLIENT_READY_USER_RETEST_PENDING**

**原因**:
- ✅ 本地 Hermes Agent 客户端已实现
- ✅ Bot 已集成本地 Hermes Agent
- ✅ 环境变量已配置
- ✅ 生产服务器已部署
- ⏳ 等待用户真实 Telegram 测试

**下一步**:
1. 用户发送 reference rewrite 测试消息
2. 验证 dry-run 响应
3. 尝试自然语言修改
4. 确认创建 topic draft
5. 验证 production topic draft
6. 生成最终报告

---

## 报告路径

`reports/project-audit/v1.20.42.18.6.16.6.84.4.9-local-hermes-agent-contentops-e2e-report.md`

---

**最终输出**: CONTENTOPS_V2_LOCAL_HERMES_CLIENT_READY_USER_RETEST_PENDING
