# v1.20.42.18.6.16.6.12 Volcengine Agent Plan Hermes Config Switch Report

**任务版本：** v1.20.42.18.6.16.6.12  
**任务名称：** Volcengine Agent Plan Hermes Config Switch  
**执行时间：** 2026-06-28 13:12  
**执行模型：** qwen3.7-plus (alibaba)  
**目标：** 切换 Hermes 到火山 Agent Plan，验证 glm-5.2 可用

---

## 一、安全合规检查

### 禁止操作检查

| 检查项 | 结果 | 说明 |
|--------|------|------|
| **是否修改项目代码** | ❌ 否 | 仅修改 Hermes 配置 |
| **是否触碰 production** | ❌ 否 | 未连接 production 服务器 |
| **是否合并 main** | ❌ 否 | 未执行 git merge |
| **是否执行部署** | ❌ 否 | 未执行任何部署 |
| **是否输出 secret** | ❌ 否 | API Key 未输出 |
| **是否执行 prisma db push** | ❌ 否 | 未执行 |
| **是否执行 destructive SQL** | ❌ 否 | 未执行 |

### 安全声明

- ✅ **Production 未触碰**
- ✅ **Main 未合并**
- ✅ **无 Production SSH**
- ✅ **无 Production 部署**
- ✅ **无 Secret 输出**

---

## 二、配置变更摘要

### 原配置（切换前）

```yaml
model:
  provider: alibaba
  default: qwen3.7-plus
  base_url: https://coding.dashscope.aliyuncs.com/v1

providers:
  volcengine:
    base_url: https://ark.cn-beijing.volces.com/api/coding/v3
    api_key: ${VOLCENGINE_API_KEY}
    models:
    - glm-5.2
```

| 配置项 | 值 |
|--------|-----|
| **原 provider** | alibaba |
| **原 default model** | qwen3.7-plus |
| **原 base_url** | https://coding.dashscope.aliyuncs.com/v1 |
| **原 volcengine base_url** | https://ark.cn-beijing.volces.com/api/coding/v3 (Coding Plan) |
| **原 volcengine models** | [glm-5.2] |
| **VOLCENGINE_API_KEY** | SET ✅ |

### 新配置（切换后）

```yaml
model:
  provider: volcengine
  default: glm-5.2

providers:
  volcengine:
    base_url: https://ark.cn-beijing.volces.com/api/plan/v3
    api_key: ${VOLCENGINE_API_KEY}
    models:
    - glm-5.2
    - deepseek-v4-pro
    - ark-code-latest
```

| 配置项 | 值 |
|--------|-----|
| **新 provider** | volcengine |
| **新 default model** | glm-5.2 |
| **新 base_url** | https://ark.cn-beijing.volces.com/api/plan/v3 (Agent Plan) |
| **新 volcengine models** | [glm-5.2, deepseek-v4-pro, ark-code-latest] |
| **VOLCENGINE_API_KEY** | SET ✅ |

### 配置变更说明

| 变更项 | 原值 | 新值 | 说明 |
|--------|------|------|------|
| **model.provider** | alibaba | volcengine | 切换到火山引擎 |
| **model.default** | qwen3.7-plus | glm-5.2 | 默认模型改为 GLM-5.2 |
| **volcengine.base_url** | /api/coding/v3 | /api/plan/v3 | 从 Coding Plan 升级到 Agent Plan |
| **volcengine.models** | [glm-5.2] | [glm-5.2, deepseek-v4-pro, ark-code-latest] | 添加备用模型 |

---

## 三、配置验证

### hermes doctor 结果

```
┌─────────────────────────────────────────────────────────┐
│                 🩺 Hermes Doctor                        │
└─────────────────────────────────────────────────────────┘

◆ Security Advisories
  ✓ No active security advisories

◆ Python Environment
  ✓ Python 3.11.15
  ✓ Virtual environment active
  ✓ Version files consistent (0.16.0)

◆ Required Packages
  ✓ OpenAI SDK
  ✓ Rich (terminal UI)
  ✓ python-dotenv
  ✓ PyYAML
  ✓ HTTPX
  ✓ Croniter (cron expressions) (optional)
  ✓ python-telegram-bot (optional)
  ✓ discord.py (optional)

◆ Configuration Files
  ✓ ~/.hermes/.env file exists
  ✓ API key or custom endpoint configured
  ✓ ~/.hermes/config.yaml exists
  ✓ Config version up to date (v27)

◆ xAI Model Retirement (May 15, 2026)
  ✓ No retired xAI models in config

◆ Directory Structure
  ✓ ~/.hermes directory exists
  ✓ ~/.hermes/cron/ exists
  ✓ ~/.hermes/sessions/ exists
  ✓ ~/.hermes/logs/ exists
  ✓ ~/.hermes/skills/ exists
  ✓ ~/.hermes/memories/ exists
  ✓ ~/.hermes/SOUL.md exists (persona configured)
```

**验证结果：** ✅ 配置验证通过

---

## 四、只读仓库测试

### 测试结果

```bash
=== pwd ===
/Users/chq/xixiong-saas

=== git branch --show-current ===
staging

=== git status --short ===
(clean)

=== git log --oneline -5 ===
c85e667 docs: v18.6.16.6.11 add model switch handoff
8ad9029 docs: v18.6.16.6.10 canvas usability stabilization report
e30536b test: v18.6.16.6.10 audit canvas real usability stabilization
63e9ee4 fix: v18.6.16.6.10 stabilize canvas product table save print ipad
c4b92a4 feat: v18.6.16.6.9 add canvas undo/redo and real product table
```

**测试结果：** ✅ 仓库状态正常

### 交接文档读取

- ✅ 交接文档已读取（前 80 行）
- ✅ 文档路径：`reports/project-audit/v1.20.42.18.6.16.6.11-model-switch-handoff-to-volcengine-agent-plan.md`
- ✅ 文档包含项目身份、安全规则、Canvas 状态、模型切换目标等信息

---

## 五、模型调用测试

### PHASE 4 — glm-5.2 调用测试

**状态：** ⏳ **待新会话测试**

**说明：**  
当前会话使用的是 qwen3.7-plus 模型，无法在当前会话中测试 glm-5.2。  
需要用户启动新的 Hermes 会话，新会话将自动使用 glm-5.2 模型。

**新会话测试步骤：**

1. 启动新 Hermes 会话：
   ```bash
   hermes
   ```

2. 发送简单问题：
   ```
   请只回复 VOLCENGINE_GLM52_OK
   ```

3. 期望回复：
   ```
   VOLCENGINE_GLM52_OK
   ```

4. 如果成功，输出：
   ```
   VOLCENGINE_GLM52_OK
   ```

5. 如果失败，记录错误类型，不要重试超过 2 次，立即回滚配置。

### PHASE 6 — 新模型接手确认

**状态：** ⏳ **待新会话确认**

**说明：**  
新模型接手后，必须执行以下步骤：

1. 读取交接文档：
   ```bash
   cat reports/project-audit/v1.20.42.18.6.16.6.11-model-switch-handoff-to-volcengine-agent-plan.md
   ```

2. 输出确认：
   ```
   READ_HANDOFF_CONFIRMED
   ```

3. 总结当前状态：
   - 当前项目：jueshi.net / xixiong-saas
   - 当前模式：DEV (staging only)
   - 禁止事项：不得触碰 production、不得合并 main、不得输出 secret
   - Canvas 当前真实状态：v18.6.16.6.10 声称已修复，但需要用户实测确认
   - 下一步：必须等待用户人工验收，不得自动继续开发

**在输出 READ_HANDOFF_CONFIRMED 之前，新模型不得修改代码、不得运行写操作、不得提交 commit、不得部署。**

### PHASE 7 — ark-code-latest 测试

**状态：** ⏳ **待新会话测试（可选）**

**说明：**  
仅在 glm-5.2 已通过后，才允许测试 ark-code-latest。

**测试步骤：**

1. 临时切换模型到 ark-code-latest：
   ```bash
   hermes --model ark-code-latest
   ```

2. 发送简单问题：
   ```
   请只回复 ARK_CODE_LATEST_OK
   ```

3. 期望回复：
   ```
   ARK_CODE_LATEST_OK
   ```

4. 如果成功，记录：
   ```
   ARK_CODE_LATEST_OK
   ```

5. 如果失败，记录失败原因。

**注意：**  
- 不要把默认模型切成 ark-code-latest
- 是否真正走 Auto，需要后续到火山控制台模型调用明细确认
- 本轮不得主观声称 Auto 已验证

---

## 六、回滚方案

### 备份文件

| 备份文件 | 路径 |
|----------|------|
| **备份 1** | `~/.hermes/config.yaml.backup.20260628-130225` |
| **备份 2** | `~/.hermes/config.yaml.backup.20260628-131230` |

### 回滚命令

如果新模型配置失败，可以回滚：

```bash
cp ~/.hermes/config.yaml.backup.20260628-131230 ~/.hermes/config.yaml
```

### 回滚条件

- ❌ 如果 glm-5.2 调用失败
- ❌ 如果 hermes doctor 报错
- ❌ 如果新模型无法正常工作

---

## 七、最终状态

### 配置切换状态

| 项目 | 状态 |
|------|------|
| **配置是否已切换** | ✅ 是 |
| **hermes doctor 是否通过** | ✅ 是 |
| **glm-5.2 是否已测试** | ⏳ 待新会话测试 |
| **ark-code-latest 是否已测试** | ⏳ 待新会话测试（可选） |
| **READ_HANDOFF_CONFIRMED 是否完成** | ⏳ 待新会话确认 |

### Git 状态

| 项目 | 值 |
|------|-----|
| **当前分支** | staging |
| **当前 commit** | c85e667 |
| **Git status** | Clean |
| **是否已提交报告** | ⏳ 待提交 |

### 配置摘要

| 配置项 | 值 |
|--------|-----|
| **Provider** | volcengine |
| **Default Model** | glm-5.2 |
| **Base URL** | https://ark.cn-beijing.volces.com/api/plan/v3 |
| **Models** | [glm-5.2, deepseek-v4-pro, ark-code-latest] |
| **VOLCENGINE_API_KEY** | SET ✅ |

---

## 八、下一步操作

### 用户操作

1. **启动新 Hermes 会话**：
   ```bash
   hermes
   ```

2. **测试 glm-5.2**：
   ```
   请只回复 VOLCENGINE_GLM52_OK
   ```

3. **确认新模型接手**：
   - 新模型必须读取交接文档
   - 新模型必须输出 `READ_HANDOFF_CONFIRMED`

4. **（可选）测试 ark-code-latest**：
   ```bash
   hermes --model ark-code-latest
   ```

### 如果测试失败

1. 回滚配置：
   ```bash
   cp ~/.hermes/config.yaml.backup.20260628-131230 ~/.hermes/config.yaml
   ```

2. 重启 Hermes 会话

---

## 九、报告提交

### 提交命令

```bash
git add reports/project-audit/v1.20.42.18.6.16.6.12-volcengine-agent-plan-config-switch-report.md
git commit -m "docs: v18.6.16.6.12 record volcengine agent plan switch"
```

### 提交信息

- **文档路径：** `reports/project-audit/v1.20.42.18.6.16.6.12-volcengine-agent-plan-config-switch-report.md`
- **Commit Message：** `docs: v18.6.16.6.12 record volcengine agent plan switch`

---

## 十、最终输出

### 配置切换状态

**VOLCENGINE_AGENT_PLAN_CONFIG_READY**

| 项目 | 值 |
|------|-----|
| **报告路径** | `reports/project-audit/v1.20.42.18.6.16.6.12-volcengine-agent-plan-config-switch-report.md` |
| **Commit Hash** | 待提交 |
| **Git Status** | Clean ✅ |
| **Default Model** | glm-5.2 |
| **Base URL** | https://ark.cn-beijing.volces.com/api/plan/v3 |
| **glm-5.2 测试结果** | ⏳ 待新会话测试 |
| **ark-code-latest 测试结果** | ⏳ 待新会话测试（可选） |
| **READ_HANDOFF_CONFIRMED** | ⏳ 待新会话确认 |
| **是否需要用户重新打开 Hermes 会话** | ✅ 是 |
| **回滚方式** | `cp ~/.hermes/config.yaml.backup.20260628-131230 ~/.hermes/config.yaml` |

---

**报告生成完成时间：** 2026-06-28 13:12  
**生成模型：** qwen3.7-plus (alibaba)  
**报告版本：** v1.20.42.18.6.16.6.12
