# v1.20.42.18.6.16.6.84.3.7-01 Real Telegram Handler Env Audit

**Date:** 2026-07-03  
**Version:** v1.20.42.18.6.16.6.84.3.7  
**Mode:** STOP_BLIND_TESTING_IDENTIFY_REAL_TELEGRAM_HANDLER

---

## Real Telegram Handler Identification

**REAL_TELEGRAM_HANDLER:** `pm2:jueshi-contentops-bot`

- **PM2 Process ID:** 3
- **PID:** 271531 (as of 2026-07-03 17:31)
- **Script Path:** `/home/deploy/xixiong-saas/start-contentops-bot.sh`
- **CWD:** `/home/deploy/xixiong-saas`
- **Git Commit:** fe4811d (v1.20.42.18.6.16.6.81)

---

## Multiple Bot Instances Check

**Result:** ✅ Only ONE bot instance running

```
deploy    271531  0.3  0.9 1196796 75576 ?       Ssl  17:31   0:00 npm exec tsx scripts/contentops/contentops-telegram-bot.ts
deploy    271543  0.0  0.0   2800  1664 ?        S    17:31   0:00 sh -c tsx scripts/contentops/contentops-telegram-bot.ts
deploy    271544  0.1  0.7 1047708 59656 ?       Sl   17:31   0:00 node /home/deploy/xixiong-saas/node_modules/.bin/tsx scripts/contentops/contentops-telegram-bot.ts
deploy    271555  0.4  1.1 43672396 92536 ?      Sl   17:31   0:00 /usr/bin/node --require /home/deploy/xixiong-saas/node_modules/tsx/dist/preflight.cjs --import file:///home/deploy/xixiong-saas/node_modules/tsx/dist/loader.mjs scripts/contentops/contentops-telegram-bot.ts
```

No local Hermes gateway running. No multiple bot instances polling.

---

## 16:57 Message Hit Check

**Result:** ✅ 16:57 message appeared in production PM2 logs

```
3|jueshi-c | Message from 8602323654: 帮我创建一个"新加坡留学生租房注意事项清单"。
3|jueshi-c | 目标用户：新加坡留学生。
3|jueshi-c | 目标国家：新加坡。
3|jueshi-c | 受众阶...
3|jueshi-c | Message from 8602323654: 确认创建...
```

---

## Environment Loading Order Audit

### Startup Script: `/home/deploy/xixiong-saas/start-contentops-bot.sh`

```bash
#!/bin/bash
# Load production environment first, then bot-specific overrides
set -a

# Load main app production env (includes DATABASE_URL)
[ -f .env.production ] && source .env.production

# Save DATABASE_URL from production
PROD_DATABASE_URL=$DATABASE_URL

# Load bot-specific overrides (must NOT override DATABASE_URL)
[ -f .env.contentops ] && source .env.contentops

# Restore production DATABASE_URL (bot must use same DB as main app)
export DATABASE_URL=$PROD_DATABASE_URL

set +a
export NODE_ENV=production

echo "Bot starting with production DATABASE_URL"
echo "CONTENTOPS_DRAFT_ALLOW_PRODUCTION=$CONTENTOPS_DRAFT_ALLOW_PRODUCTION"

# Start the bot
exec npx tsx scripts/contentops/contentops-telegram-bot.ts
```

### Environment Variables Source

1. **DATABASE_URL:** Loaded from `.env.production` (main app production env)
2. **CONTENTOPS_TELEGRAM_BOT_TOKEN:** Loaded from `.env.contentops`
3. **CONTENTOPS_DRAFT_ALLOW_PRODUCTION:** Loaded from `.env.contentops`
4. **Other CONTENTOPS_* vars:** Loaded from `.env.contentops`

### Process Environment Verification

```bash
$ cat /proc/271531/environ | tr '\0' '\n' | grep -E 'DATABASE_URL|CONTENTOPS'
DATABASE_URL=postgresql://bxb_user:***@Secure@127.0.0.1:5432/bxb_prod?schema=public
CONTENTOPS_PUBLICATION_ALLOWED=false
CONTENTOPS_TELEGRAM_RATE_LIMIT_MS=5000
CONTENTOPS_BOT_ENABLED=true
CONTENTOPS_DRAFT_ALLOW_PRODUCTION=false
CONTENTOPS_TELEGRAM_429_COOLDOWN_MINUTES=30
CONTENTOPS_TELEGRAM_BOT_TOKEN=889768...XLnY
CONTENTOPS_TELEGRAM_ALLOWED_CHAT_IDS=8602323654
CONTENTOPS_TELEGRAM_MAX_MESSAGES_PER_MINUTE=5
```

---

## DATABASE_URL Consistency Check

**Result:** ✅ DATABASE_URL same as main app

- **Main App (.env.production):** `postgresql://bxb_user:***@Secure@127.0.0.1:5432/bxb_prod?schema=public`
- **Bot (runtime):** `postgresql://bxb_user:***@Secure@127.0.0.1:5432/bxb_prod?schema=public`
- **Consistency:** ✅ SAME

---

## Stale Credentials Check

**Result:** ✅ NOT using stale credentials

- Previous issue: `.env.contentops` had incorrect DATABASE_URL with wrong password
- Fix: Removed DATABASE_URL from `.env.contentops`, bot now inherits from `.env.production`
- Current status: Bot uses correct production DATABASE_URL

---

## Prisma Client Initialization Order

**Result:** ✅ Prisma Client initialized AFTER dotenv loading

Bot code flow:
1. Startup script loads `.env.production` and `.env.contentops`
2. Environment variables are exported
3. Bot script starts with `exec npx tsx scripts/contentops/contentops-telegram-bot.ts`
4. Bot code reads `process.env.DATABASE_URL` at runtime (not at module load time)
5. Prisma Client is initialized inside `handleConfirmCreate()` method with correct DATABASE_URL

---

## Summary

| Check | Status |
|-------|--------|
| Real Telegram handler identified | ✅ pm2:jueshi-contentops-bot |
| Only one bot instance running | ✅ Yes |
| 16:57 message hit production PM2 | ✅ Yes |
| DATABASE_URL same as main app | ✅ Yes |
| Not using stale credentials | ✅ Yes |
| Prisma Client initialized after dotenv | ✅ Yes |
| Environment variables correctly loaded | ✅ Yes |

---

## Conclusion

**ENV_AUDIT_PASS**

The real Telegram handler is correctly identified as `pm2:jueshi-contentops-bot`. Environment variables are correctly loaded from `.env.production` (for DATABASE_URL) and `.env.contentops` (for bot-specific vars). The bot is using the same DATABASE_URL as the main app, and Prisma Client is initialized after environment variables are loaded.
