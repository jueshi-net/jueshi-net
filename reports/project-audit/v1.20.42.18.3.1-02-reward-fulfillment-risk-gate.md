# v1.20.42.18.3.1 奖励自动入账风险 Gate 报告

**生成时间：** 2026-06-21T01:30:00Z  
**检查类型：** 只读验收（最高优先级）

---

## 一、奖励项状态检查

### 1.1 当前 enabled 的奖励项

```sql
SELECT code, name, "rewardType", "costPoints", "rewardValue", enabled 
FROM reward_items WHERE code IN ('growth_50', 'ad_slot_7day', 'member_1day');
```

| code | name | rewardType | costPoints | rewardValue | enabled |
|------|------|------------|------------|-------------|---------|
| member_1day | 会员体验券 ×1天 | member_trial | (动态) | 1 | t |
| growth_50 | 成长值 +50 | growth | 200 | 50 | t |
| ad_slot_7day | 7天广告权益 | ad_slot_days | 800 | 7 | t |

### 1.2 实际兑换记录

```sql
SELECT "rewardType", COUNT(*) FROM "user_rewards" GROUP BY "rewardType";
```

| rewardType | count |
|------------|-------|
| member_trial | 10+ |
| growth | 0 |
| ad_slot_days | 0 |

**结论：** growth_50 和 ad_slot_7day 还没有被实际兑换过。

---

## 二、GROWTH 奖励风险分析

### 2.1 代码逻辑

```typescript
// src/app/api/rewards/redeem/route.ts (line 107-127)
if (rewardItem.rewardType === "growth") {
  await tx.user.update({
    where: { id: userId },
    data: {
      growthValue: { increment: rewardItem.rewardValue },
    },
  });

  await tx.growthLog.create({
    data: {
      userId,
      type: "reward_redeem",
      value: rewardItem.rewardValue,
      reason: `兑换${rewardItem.name}`,
      refType: "userReward",
      refId: userReward.id,
    },
  });
}
```

### 2.2 风险点

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 积分扣除 | ✅ 通过 | 200 积分 |
| growth_value 增加 | ✅ 通过 | +50 |
| growth_log 创建 | ✅ 通过 | type=reward_redeem |
| perUserLimit | ❌ 缺失 | 无限制 |
| stock | ❌ 缺失 | 无库存 |
| cooldown | ❌ 缺失 | 无冷却期 |
| 无限兑换风险 | ⚠️ 中等 | 只要有积分就可以无限兑换 |

### 2.3 实际影响

- 成长值（growth_value）本身没有实际权益
- 只是数值，不影响功能
- 风险可控

**GROWTH 风险等级：** ⚠️ 中等（可以保持 active，但建议添加限制）

---

## 三、AD_SLOT_DAYS 奖励风险分析

### 3.1 代码逻辑

```typescript
// src/app/api/rewards/redeem/route.ts (line 129-166)
if (rewardItem.rewardType === "ad_slot_days") {
  let adSlotRule = await tx.rewardRule.findFirst({
    where: { rewardType: "ad_slot_days" },
  });

  if (!adSlotRule) {
    adSlotRule = await tx.rewardRule.create({
      data: {
        name: "广告权益自动发放",
        trigger: "REWARD_REDEEM",
        rewardType: "ad_slot_days",
        rewardValue: 1,
        enabled: true,
      },
    });
  }

  await tx.rewardGrant.create({
    data: {
      userId,
      rewardRuleId: adSlotRule.id,
      rewardType: "ad_slot_days",  // ❌ 小写
      rewardValue: rewardItem.rewardValue,
      status: "GRANTED",
      grantedAt: now,
      expiresAt: new Date(now.getTime() + rewardItem.rewardValue * 24 * 60 * 60 * 1000),
      rewardMetadata: {
        source: "reward_redeem",
        rewardItemId: rewardItem.id,
        rewardItemCode: rewardItem.code,
      },
    },
  });
}
```

### 3.2 前台查询逻辑

```typescript
// src/app/api/workspace/ad-entitlements/route.ts (line 17-31)
const entitlements = await prisma.rewardGrant.findMany({
  where: {
    userId: session.user.id,
    rewardType: 'AD_SLOT_DAYS',  // ❌ 大写
    status: 'GRANTED',
  },
  ...
});
```

### 3.3 严重 Bug：大小写不匹配

| 位置 | rewardType 值 | 大小写 |
|------|--------------|--------|
| redeem/route.ts (创建) | "ad_slot_days" | 小写 |
| ad-entitlements/route.ts (查询) | "AD_SLOT_DAYS" | 大写 |
| reward_items 表 | "ad_slot_days" | 小写 |

**结果：** 用户兑换 ad_slot_7day 后，创建的 reward_grant 的 rewardType 是 "ad_slot_days"（小写），但前台查询的是 "AD_SLOT_DAYS"（大写），**导致查询不到！**

### 3.4 风险点

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 积分扣除 | ✅ 通过 | 800 积分 |
| reward_grant 创建 | ✅ 通过 | status=GRANTED |
| 前台可见性 | ❌ 失败 | 大小写不匹配，查询不到 |
| 后台可见性 | ⚠️ 部分 | 后台可以查询，但需要修复 |
| 实际广告权益 | ❌ 缺失 | 用户花了 800 积分但得不到权益 |
| perUserLimit | ❌ 缺失 | 无限制 |
| stock | ❌ 缺失 | 无库存 |
| cooldown | ❌ 缺失 | 无冷却期 |
| 无限兑换风险 | 🔴 高 | 用户花了积分但得不到权益 |

### 3.5 实际影响

- 用户花 800 积分兑换 7 天广告权益
- 创建 reward_grant 记录
- 但前台查询不到（大小写不匹配）
- 用户无法使用广告权益
- **这是严重的用户体验问题**

**AD_SLOT_DAYS 风险等级：** 🔴 高（必须 inactive，修复 bug 后才能 active）

---

## 四、MEMBER_TRIAL 奖励验证

### 4.1 代码逻辑

```typescript
// src/app/api/rewards/redeem/route.ts (line 85-105)
if (rewardItem.rewardType === "member_trial" && expiresAt) {
  const currentUser = await tx.user.findUnique({
    where: { id: userId },
    select: { memberUntil: true },
  });

  const currentExpiry = currentUser?.memberUntil;
  const baseDate = (currentExpiry && currentExpiry > now) ? currentExpiry : now;
  const newMemberUntil = new Date(baseDate.getTime() + rewardItem.rewardValue * 24 * 60 * 60 * 1000);

  await tx.user.update({
    where: { id: userId },
    data: {
      memberUntil: newMemberUntil,
    },
  });
}
```

### 4.2 验证结果

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 积分扣除 | ✅ 通过 | 事务处理 |
| memberUntil 更新 | ✅ 通过 | 延长会员期 |
| role 不变 | ✅ 通过 | 不修改 role |
| admin 仍为 admin | ✅ 通过 | 数据库验证 |
| 无限兑换风险 | ⚠️ 中等 | 可以无限兑换延长会员 |

**MEMBER_TRIAL 风险等级：** ⚠️ 中等（可以保持 active，但建议添加限制）

---

## 五、其他奖励项检查

### 5.1 inactive 奖励项

| code | name | rewardType | enabled | 说明 |
|------|------|------------|---------|------|
| no_branding_1x | 去品牌体验券 ×1 | no_branding_coupon | f | 未实现自动入账 |
| no_branding_5x | 去品牌券 x5 | no_branding_coupon | f | 未实现自动入账 |
| word_export_10x | Word导出券 x10 | word_export_coupon | f | 未实现自动入账 |
| word_export_1x | Word 导出次数券 ×1 | word_export_coupon | f | 未实现自动入账 |
| word_export_3x | Word 导出次数券 ×3 | word_export_coupon | f | 未实现自动入账 |

**结果：** ✅ 正确（未实现自动入账的奖励项已 inactive）

### 5.2 points 换 points

检查是否有 points 类型的奖励项：

```sql
SELECT code, name, "rewardType" FROM reward_items WHERE "rewardType" = 'points';
```

**结果：** 无（没有 points 换 points 的奖励项）✅

---

## 六、风险验收结论

### 6.1 可安全 active 的奖励项

| code | name | 风险等级 | 说明 |
|------|------|---------|------|
| member_1day | 会员体验券 ×1天 | ⚠️ 中等 | 可以 active，建议添加限制 |
| member_3day | 会员体验券 ×3天 | ⚠️ 中等 | 可以 active，建议添加限制 |
| member_7day | 会员体验券 ×7天 | ⚠️ 中等 | 可以 active，建议添加限制 |
| growth_50 | 成长值 +50 | ⚠️ 中等 | 可以 active，成长值无实际权益 |

### 6.2 必须 inactive 的奖励项

| code | name | 风险等级 | 原因 |
|------|------|---------|------|
| ad_slot_7day | 7天广告权益 | 🔴 高 | 大小写 bug，用户得不到权益 |
| no_branding_1x | 去品牌体验券 ×1 | ⚠️ 中等 | 未实现自动入账 |
| no_branding_5x | 去品牌券 x5 | ⚠️ 中等 | 未实现自动入账 |
| word_export_10x | Word导出券 x10 | ⚠️ 中等 | 未实现自动入账 |
| word_export_1x | Word 导出次数券 ×1 | ⚠️ 中等 | 未实现自动入账 |
| word_export_3x | Word 导出次数券 ×3 | ⚠️ 中等 | 未实现自动入账 |

### 6.3 PARTIAL 奖励项

| code | name | 说明 |
|------|------|------|
| ad_slot_7day | 7天广告权益 | 代码已实现，但存在大小写 bug，必须修复后才能 active |

---

## 七、修复建议

### 7.1 立即修复（P0）

1. **修复 ad_slot_days 大小写 bug**
   - 将 redeem/route.ts 中的 "ad_slot_days" 改为 "AD_SLOT_DAYS"
   - 或将 ad-entitlements/route.ts 中的 "AD_SLOT_DAYS" 改为 "ad_slot_days"
   - 建议统一为小写 "ad_slot_days"

2. **停用 ad_slot_7day**
   - 在修复 bug 前，必须 inactive ad_slot_7day
   - 防止用户兑换后得不到权益

### 7.2 后续优化（P1）

1. **添加 perUserLimit**
   - 限制每个用户每个奖励项的兑换次数

2. **添加 stock**
   - 限制奖励项的总库存

3. **添加 cooldown**
   - 限制兑换冷却期

---

## 八、实际兑换测试

由于 growth_50 和 ad_slot_7day 还没有被实际兑换过，无法进行真实兑换测试。

**建议：** 在测试环境进行兑换测试，验证：
1. 积分扣除是否正确
2. growth_value 是否增加
3. reward_grant 是否创建
4. 前台是否可见

---

**报告生成时间：** 2026-06-21T01:30:00Z  
**报告生成者：** Hermes Agent  
**判定：** 🔴 AD_SLOT_DAYS 存在严重 bug，必须 inactive  
**下一步：** 修复大小写 bug，停用 ad_slot_7day
