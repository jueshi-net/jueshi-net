# v1.20.42.9.0 阶段 2：会员权益与免费用户限制提示闭环

**版本**: v1.20.42.9.0  
**阶段**: 阶段 2 - 会员权益与免费用户限制提示闭环  
**日期**: 2026-06-18  
**状态**: 🔄 执行中

---

## 一、现状分析

### 前端（已有）
✅ 公司资料页面有提示（第 30-38 行）：
```tsx
{!isMember && profiles.length >= 1 && (
  <div className="bg-amber-50 border border-amber-200 rounded-xl p-4">
    <p>免费版限制：最多 1 套公司资料</p>
    <p>升级会员可创建最多 10 套公司模板</p>
  </div>
)}
```

✅ 创建模态框有提示（第 116-120 行）：
```tsx
{!isMember && (
  <div className="bg-amber-50 border border-amber-100 rounded-lg p-3">
    免费版支持 1 套公司资料，不支持上传 Logo。
  </div>
)}
```

### 后端（缺失）
❌ **API POST 方法没有数量限制验证！**
- 第 67 行计算了 `existingCount`，但只用于判断是否设为默认
- 没有检查是否超过限制
- 用户可以通过 API 绕过前端限制

---

## 二、修复方案

### 修复 1：后端 API 添加数量限制验证

**文件**: `/src/app/api/me/company-profiles/route.ts`

**修改位置**: POST 方法，在验证之后、创建之前

**修改内容**:
```typescript
// 数量限制验证
const existingCount = await prisma.userCompanyProfile.count({ where: { userId } });
const maxProfiles = member ? 10 : 1; // 会员 10 个，免费版 1 个

// 管理员不受限制
const isAdmin = session.user.role === "admin" || session.user.role === "管理员";

if (!isAdmin && existingCount >= maxProfiles) {
  return NextResponse.json(
    { 
      error: `免费版最多创建 ${maxProfiles} 套公司资料，升级会员可创建 10 套`,
      limit: maxProfiles,
      current: existingCount
    },
    { status: 403 }
  );
}
```

### 修复 2：前端优化（可选）

**文件**: `/src/app/(workspace)/workspace/company-profiles/company-profiles-client.tsx`

**修改内容**:
1. 达到限制时禁用"新建"按钮
2. 点击时显示提示而非打开模态框

---

## 三、执行计划

1. ✅ 修复后端 API 添加数量限制验证
2. ⏳ 测试 API 限制是否生效
3.  验证前端提示是否正常
4.  验证管理员不受限制

---

## 四、验收标准

- [ ] 免费版用户创建第 2 个公司资料时 API 返回 403
- [ ] 会员用户创建第 11 个公司资料时 API 返回 403
- [ ] 管理员不受限制
- [ ] 前端提示正常显示
- [ ] 错误信息友好

---

**报告时间**: 2026-06-18
