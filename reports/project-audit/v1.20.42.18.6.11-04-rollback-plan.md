# v1.20.42.18.6.11-04 — Rollback Plan

**Date:** 2026-06-24
**Version:** v1.20.42.18.6.11
**CURRENT_MODE:** OPS
**TARGET_ENV:** production (https://jueshi.net)

---

## Rollback Commit

- **Pre-release commit:** `0d8213c` (production HEAD before release)
- **Post-release commit:** `4f0d947` (current production HEAD)
- **Rollback target:** `0d8213c`

## 回滚命令

```bash
# 1. SSH to production
ssh -i ~/.ssh/jueshi-prod-v2 deploy@104.250.109.99

# 2. Restore pre-release code
cd /home/deploy/xixiong-saas
git checkout 0d8213c -- src/

# 3. Rebuild
set -a && source .env.production && set +a
npx prisma generate
NODE_ENV=production npx next build

# 4. Restart PM2
pm2 restart xixiong-saas

# 5. Verify
curl -sI https://jueshi.net/ | head -3
```

## 是否需要 DB Rollback

**否** — 本次发布仅代码变更（header CSS 修复、审计脚本、文档）。所有 Prisma migrations 在本次发布前已应用到生产 DB。本次 `prisma migrate deploy` 未应用任何新 migration。Migration 是增量兼容的，无需 DB rollback。

## DB Restore 方法（如需）

如果 migration 有风险需要 DB restore：

```bash
# Backup location
ssh -i ~/.ssh/jueshi-prod-v2 deploy@104.250.109.99
cd /home/deploy/xixiong-saas
set -a && source .env.production && set +a

# Restore from backup
gunzip -c /home/deploy/backups/pre-release-20260624-082545/db-backup.sql.gz | psql "$DATABASE_URL"
```

**注意:** DB restore 会丢失发布后的所有数据变更。仅在紧急情况下使用。

## PM2 回滚步骤

1. `pm2 stop xixiong-saas` — 停止当前进程
2. 恢复旧代码（git checkout 或 rsync）
3. `npx prisma generate` — 重新生成 Prisma client
4. `NODE_ENV=production npx next build` — 重新构建
5. `pm2 restart xixiong-saas` — 重启
6. `curl -sI https://jueshi.net/` — 验证 200

## Nginx 无变化确认

**确认** — 本次发布未修改 Nginx 配置。Nginx 配置文件无变化。

## 备份路径

- **DB dump:** `/home/deploy/backups/pre-release-20260624-082545/db-backup.sql.gz` (138MB)
- **Code HEAD:** `0d8213c` (记录于 `/home/deploy/backups/pre-release-20260624-082545/git-head.txt`)
- **PM2 save:** `/home/deploy/.pm2/dump.pm2`
- **iCloud backup:** Hermes cron 每天 03:00 自动备份到 Mac iCloud，30天保留

## 触发回滚条件

| 条件 | 说明 |
|------|------|
| 首页 500/502 | `https://jueshi.net/` 返回 500 或 502 |
| 核心页面 500/502 | /destinations/canada, /tools/postal-code, /bbs, /login 返回 500/502 |
| 登录完全失败 | 所有用户无法登录 |
| DB 连接失败 | Prisma 无法连接 DB |
| PM2 不稳定 | unstable restarts > 0 或持续 crash loop |
| 静态资源大量失败 | CSS/JS/图片 404 率 > 10% |
| Production admin 权限异常 | admin 无法访问 /admin 或权限丢失 |
| 9833416@qq.com 异常 | 账号被修改/删除/角色变更 |
| role=member 非 0 | 出现 role=member 的用户 |
| 用户要求回滚 | 用户明确要求回滚 |

## 回滚后验证

1. `curl -sI https://jueshi.net/` — 200
2. `curl -sI https://jueshi.net/bbs` — 200
3. `curl -sI https://jueshi.net/login` — 200
4. `ssh ... 'pm2 status'` — online, unstable=0
5. `ssh ... 'node -e "..."` — role=member=0, 9833416@qq.com=admin
