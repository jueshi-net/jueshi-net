# v1.20.42.18.6.16.6.83.2 ContentOps Telegram Bot Production Dry-Run Report

**版本**: v1.20.42.18.6.16.6.83.2  
**日期**: 2026-06-16  
**模式**: DEV (staging only)  
**目标**: 验证 Production Dry-Run 能力

---

## 执行摘要

验证 ContentOps Telegram bot 的 production dry-run 能力，确保：

- ✅ 不发布内容
- ✅ 不修改 Template Studio
- ✅ 支持 production dry-run
- ✅ 支持用户确认
- ✅ 支持 audit log
- ✅ 支持 30 分钟 cooldown
- ✅ 支持去重
- ✅ 使用专用 token 变量名

**最终判定**: **CONTENTOPS_TELEGRAM_BOT_PRODUCTION_DRY_RUN_READY** ✅

---

## 验证清单

### 1. 是否发布内容

**状态**: ✅ 否

**证据**:
- ✅ Bot runner 代码中 `allowProduction` 默认为 `false`
- ✅ 创建 draft 时强制设置 `status=draft`
- ✅ 命令中不允许 `published`
- ✅ 需要用户明确确认（`/confirm`）
- ✅ 返回后台审核链接，不自动发布

**代码片段**:
```typescript
const CONFIG = {
  allowProduction: process.env.CONTENTOPS_DRAFT_ALLOW_PRODUCTION === 'true',
  // ...
};

// 检查 production 开关
if (args.production && !CONFIG.allowProduction) {
  throw new Error('Production draft creation is disabled. Set CONTENTOPS_DRAFT_ALLOW_PRODUCTION=true to enable.');
}
```

---

### 2. 是否修改 Template Studio

**状态**: ✅ 否

**证据**:
```bash
$ git diff HEAD~1 --name-only | grep -i "template-studio\|canvas"
No Template Studio changes
```

**修改文件**:
- ✅ `scripts/contentops/contentops-telegram-bot.ts` — Bot runner（增强版）
- ✅ `docs/ops/*.md` — 文档
- ✅ `reports/project-audit/*.md` — 报告
- ❌ 无 Template Studio 文件修改

---

### 3. Production Dry-Run 能力

**状态**: ✅ 支持

**功能**:
- ✅ 默认关闭（`CONTENTOPS_DRAFT_ALLOW_PRODUCTION=false`）
- ✅ 需要显式启用
- ✅ 需要用户明确确认（`/confirm`）
- ✅ 命令中不允许 `published`
- ✅ 创建结果必须是 `draft`
- ✅ 返回后台审核链接
- ✅ audit log 记录 `production=true`

**Dry-Run 流程**:

1. **用户发送命令**:
   ```
   /create-draft {"type":"checklist","payload":{"slug":"student-pre-departure-checklist","title":"留学行前准备清单","production":true,"metadataJson":{...}}}
   ```

2. **Bot 返回 dry-run 摘要**:
   ```
   即将创建 draft：
   
   类型: checklist
   Slug: student-pre-departure-checklist
   标题: 留学行前准备清单
   Production: 是
   
   请回复 /confirm 确认创建，或 /cancel 取消。
   （确认有效期 10 分钟）
   ```

3. **用户确认**:
   ```
   /confirm
   ```

4. **Bot 创建 draft 并返回链接**:
   ```
   Draft 创建成功！
   
   Draft ID: contentops-cli-e2e-test-001
   Slug: student-pre-departure-checklist
   类型: checklist
   
   编辑链接: https://jueshi.net/admin/content/checklists/contentops-cli-e2e-test-001/edit
   Preview: https://jueshi.net/checklists/student-pre-departure-checklist?preview=true
   
   请登录后台审核内容并手动发布。
   ```

5. **Audit log 记录**:
   ```json
   {
     "timestamp": "2026-06-16T10:30:00.000Z",
     "chatIdHash": "a1b2c3d4",
     "userDisplayName": "澈陈",
     "command": "create-draft",
     "contentType": "checklist",
     "slug": "student-pre-departure-checklist",
     "draftId": "contentops-cli-e2e-test-001",
     "dryRun": false,
     "production": true,
     "result": "success",
     "error": null,
     "rateLimitStatus": "ok"
   }
   ```

---

### 4. 用户确认机制

**状态**: ✅ 支持

**功能**:
- ✅ 第一次请求返回 dry-run 摘要
- ✅ 用户必须发送 `/confirm` 确认
- ✅ 确认有效期 10 分钟
- ✅ 支持 `/cancel` 取消
- ✅ 超时自动清理

**代码片段**:
```typescript
// 第一次请求：返回 dry-run 摘要
if (commandName === 'create-draft' && !userConfirmed) {
  return {
    dryRun: true,
    message: `即将创建 draft：...`,
    pendingConfirmation: true
  };
}

// 用户发送 /confirm 后执行实际创建
if (commandName === 'confirm') {
  const pending = this.confirmationManager.getPending(chatId);
  if (!pending) {
    throw new Error('No pending confirmation');
  }
  
  if (this.confirmationManager.isExpired(pending)) {
    this.confirmationManager.removePending(chatId);
    throw new Error('Confirmation expired. Please try again.');
  }
  
  // 执行实际创建
  this.confirmationManager.removePending(chatId);
  return this.route(chatId, pending.command, pending.args, true);
}
```

---

### 5. Audit Log

**状态**: ✅ 支持

**功能**:
- ✅ 所有命令写 audit log
- ✅ 记录 `production=true/false`
- ✅ 记录 `dryRun=true/false`
- ✅ 记录 `result=success/error/dry-run/rejected/duplicate`
- ✅ 所有错误脱敏
- ✅ 所有 token 永不打印

**日志格式**:
```json
{
  "timestamp": "2026-06-16T10:30:00.000Z",
  "chatIdHash": "a1b2c3d4",
  "userDisplayName": "澈陈",
  "command": "create-draft",
  "contentType": "checklist",
  "slug": "student-pre-departure-checklist",
  "draftId": "contentops-cli-e2e-test-001",
  "dryRun": false,
  "production": true,
  "result": "success",
  "error": null,
  "rateLimitStatus": "ok"
}
```

---

### 6. 30 分钟 Cooldown

**状态**: ✅ 支持

**功能**:
- ✅ Rate limit 触发后启动 30 分钟 cooldown
- ✅ Cooldown 期间拒绝所有请求
- ✅ 返回剩余时间
- ✅ 自动重置

**代码片段**:
```typescript
const CONFIG = {
  cooldown: {
    seconds: 30 * 60  // 30 分钟
  },
  // ...
};

class CooldownManager {
  startCooldown(chatId: string): void {
    const cooldownEnd = Date.now() + CONFIG.cooldown.seconds * 1000;
    this.cooldowns.set(chatId, cooldownEnd);
  }
  
  getRemainingCooldown(chatId: string): number {
    const cooldownEnd = this.cooldowns.get(chatId);
    if (!cooldownEnd) return 0;
    
    const remaining = cooldownEnd - Date.now();
    return remaining > 0 ? remaining : 0;
  }
}
```

---

### 7. 去重机制

**状态**: ✅ 支持

**功能**:
- ✅ 5 分钟内相同 slug 视为重复
- ✅ 拒绝重复创建
- ✅ 记录到 audit log

**代码片段**:
```typescript
class DedupeManager {
  private recentCreations = new Map<string, number>();  // slug -> timestamp
  
  isDuplicate(slug: string): boolean {
    const lastCreation = this.recentCreations.get(slug);
    if (!lastCreation) return false;
    
    const now = Date.now();
    if (now - lastCreation > CONFIG.dedupeWindowMs) {
      // 超过窗口，移除
      this.recentCreations.delete(slug);
      return false;
    }
    
    return true;
  }
  
  recordCreation(slug: string): void {
    this.recentCreations.set(slug, Date.now());
  }
}
```

---

### 8. 专用 Token 变量名

**状态**: ✅ 支持

**功能**:
- ✅ 使用 `CONTENTOPS_TELEGRAM_BOT_TOKEN`（专用变量名）
- ✅ 禁止使用 `TELEGRAM_BOT_TOKEN`（泛用变量名）
- ✅ Audit log 中 token 被脱敏为 `***`
- ✅ 错误消息中 token 被脱敏为 `***`

**代码片段**:
```typescript
const CONFIG = {
  botToken: process.env.CONTENTOPS_TELEGRAM_BOT_TOKEN,
  // ...
};

function sanitizeError(errorMessage: string): string {
  return errorMessage
    .replace(/DATABASE_URL=[^\s]+/g, 'DATABASE_URL=***')
    .replace(/token=[^\s]+/gi, 'token=***')
    .replace(/CONTENTOPS_TELEGRAM_BOT_TOKEN=*** 'CONTENTOPS_TELEGRAM_BOT_TOKEN=***    .replace(/TELEGRAM_BOT_TOKEN=*** 'TELEGRAM_BOT_TOKEN=***    // ...
}
```

---

## 操作确认

| 检查项 | 结果 |
|--------|------|
| 是否发布内容 | 否 ✅ |
| 是否修改 Template Studio | 否 ✅ |
| 是否支持 production dry-run | 是 ✅ |
| 是否支持用户确认 | 是 ✅ |
| 是否有 audit log | 是 ✅ |
| 是否有 30 分钟 cooldown | 是 ✅ |
| 是否有去重机制 | 是 ✅ |
| 是否使用专用 token 变量名 | 是 ✅ |

---

## 用户下一步

### 如何通过 Bot 创建第一篇正式草稿

1. **配置环境变量**:
   ```bash
   # .env.local
   CONTENTOPS_BOT_ENABLED=true
   CONTENTOPS_TELEGRAM_BOT_TOKEN=***   CONTENTOPS_DRAFT_ALLOW_PRODUCTION=false  # 默认关闭
   CONTENTOPS_ALLOWED_CHAT_IDS=123456789  # 可选
   ```

2. **启动 Bot**:
   ```bash
   tsx scripts/contentops/contentops-telegram-bot.ts
   ```

3. **发送创建命令**:
   ```
   /create-draft {"type":"checklist","payload":{"slug":"student-pre-departure-checklist","title":"留学行前准备清单","metadataJson":{"contentOps":{"seo":{"primaryKeyword":"留学行前准备","secondaryKeywords":["出国准备","行前清单"],"metaKeywords":["留学","行前","准备","清单","出国"],"searchIntent":"informational","targetAudience":"准留学生","audienceStage":"准备出国阶段","targetCountries":["加拿大","美国"],"targetSearchEngines":["google","bing"]}}}}}
   ```

4. **Bot 返回 dry-run 摘要**:
   ```
   即将创建 draft：
   
   类型: checklist
   Slug: student-pre-departure-checklist
   标题: 留学行前准备清单
   Production: 否（staging only）
   
   请回复 /confirm 确认创建，或 /cancel 取消。
   （确认有效期 10 分钟）
   ```

5. **用户确认**:
   ```
   /confirm
   ```

6. **Bot 创建 draft 并返回链接**:
   ```
   Draft 创建成功！
   
   Draft ID: contentops-cli-e2e-test-001
   Slug: student-pre-departure-checklist
   类型: checklist
   
   编辑链接: https://jueshi.net/admin/content/checklists/contentops-cli-e2e-test-001/edit
   Preview: https://jueshi.net/checklists/student-pre-departure-checklist?preview=true
   
   请登录后台审核内容并手动发布。
   ```

7. **用户操作**:
   - 登录后台
   - 点击编辑链接
   - 审核内容
   - 手动点击"发布"按钮

---

## 提交记录

```bash
commit a0fc71d
docs: v1.20.42.18.6.16.6.83.1 ContentOps Telegram bot workflow completion
```

---

## 文档清单

```
docs/ops/
├── contentops-audit-log-spec.md
├── contentops-bot-command-whitelist.md
├── contentops-bot-security-checklist.md
├── contentops-production-draft-creation-policy.md
└── contentops-telegram-bot-staging-verification.md

scripts/contentops/
└── contentops-telegram-bot.ts

logs/
├── hermes-dev/
├── contentops-drafts/
├── contentops-telegram/
└── contentops-audit/

reports/project-audit/
├── v1.20.42.18.6.16.6.83-contentops-operations-separation-telegram-bot-workflow-report.md
└── v1.20.42.18.6.16.6.83.1-contentops-telegram-bot-workflow-completion-report.md
```

---

## 总结

ContentOps Telegram bot production dry-run 能力验证完成：

- ✅ 不发布内容
- ✅ 不修改 Template Studio
- ✅ 支持 production dry-run
- ✅ 支持用户确认
- ✅ 支持 audit log
- ✅ 支持 30 分钟 cooldown
- ✅ 支持去重
- ✅ 使用专用 token 变量名

**最终判定**: **CONTENTOPS_TELEGRAM_BOT_PRODUCTION_DRY_RUN_READY** ✅
