# v1.20.42.18.6.1 — Phase 1: 模型设计

## 新增模型

### UserCommunityProfile
- userId (unique 1:1), displayName, bio, locationText, websiteUrl, publicTitle
- joinedAtDisplayMode (date/month/year/hidden), isPublic

### HonorLog
- userId, delta, reason, sourceType, sourceId, actorId, createdAt
- 索引: userId, sourceType, createdAt

### CommunityStat
- userId (unique 1:1), postCount, commentCount, acceptedAnswerCount, featuredPostCount
- helpfulVoteCount, reportAcceptedCount, violationCount, lastActiveAt

## User 模型扩展
- honorScore Int @default(0) — 荣誉值（不可消费）

## Migration 安全审计
- 3 个 CREATE TABLE（新表，零风险）
- 1 个 ALTER TABLE ADD COLUMN（additive，零风险）
- 无 DROP, 无 DELETE, 无 TRUNCATE
- 不影响现有 User/role/membershipTier
