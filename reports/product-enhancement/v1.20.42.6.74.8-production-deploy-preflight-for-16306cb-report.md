# v1.20.42.6.74.8 Production Deploy Preflight for 16306cb Report

**版本**: v1.20.42.6.74.8  
**日期**: 2026-06-14  
**阶段**: Production Deploy Preflight  
**状态**: ✅ 预检通过，可以部署

---

## 合规声明

| 检查项 | 结果 |
|---|---|
| 是否部署 | **否** ✅ |
| 是否 PM2 restart | **否** ✅ |
| 是否恢复 Beta | **否** ✅ |
| 是否进入 S1 | **否** ✅ |
| 是否输出密码 | **否** ✅ |

---

## 一、远端版本确认

### 1.1 版本信息

| 项目 | 值 |
|---|---|
| origin/main HEAD | `16306cbbd6688f34b297bb8a5d671bc51816aa69` |
| Commit message | v1.20.42.6.74.6: S0 final two-gate verification before push |
| 本地与远端一致性 | ✅ 一致 |
| 未推送 commits | 无 |

### 1.2 最近 10 个 Commits

```
16306cb v1.20.42.6.74.6: S0 final two-gate verification before push
1825c13 v1.20.42.6.71-R7: Build recovery analysis & type fix
c8d264c docs: v1.20.42.6.71-R6 build gate analysis report
4e4a240 v1.20.42.6.71-R5: Workspace documentType normalization fix
a28ebdf v1.20.42.6.71-R4: Logged-in verification report
f77153f v1.20.42.6.71-R3: Container calculation wording fix & partial verification
8b68257 fix: Container/CBM volume utilization calculation bug
151ca62 v1.20.42.6.71-R: Core Tool Real Polish - Packing List/Proforma Invoice fields + example fill
0e423a2 v1.20.42.6.71: Core Tools Polish Sprint-1 - Tool recommendations enhancement
50b7f69 docs: v1.20.42.6.70 production deploy verification report
```

---

## 二、生产当前状态

### 2.1 PM2 状态

| 项目 | 值 |
|---|---|
| 进程名 | xixiong-saas |
| 状态 | ✅ online |
| 运行时间 | 20h |
| 重启次数 | 20 |
| 内存使用 | 75.8mb |

### 2.2 生产版本

| 项目 | 值 | 备注 |
|---|---|---|
| 生产 git commit | `a62743a` (v1.20.42.6.55-Y-R) | **不代表实际运行版本** |
| 实际运行版本 | `6fe3c76` (v1.20.42.6.70) | 通过 rsync 部署 |
| BUILD_ID 时间 | Jun 13 10:07 | 昨天构建 |

### 2.3 生产环境

| 项目 | 值 |
|---|---|
| Node.js | v22.22.2 ✅ |
| npm | 10.9.7 ✅ |
| 生产目录大小 | 1.8G |
| .env.production | ✅ 存在 |

### 2.4 重要说明

**VPS git commit 不代表实际运行版本**。生产代码通过 rsync 部署，不是通过 git pull。

---

## 三、VPS 临时目录 Build 验证

### 3.1 临时目录

| 项目 | 值 |
|---|---|
| 路径 | `/home/deploy/build-checks/jueshi-16306cb` |
| 代码来源 | 本地打包上传 |
| Commit | `16306cb` |

### 3.2 Build 过程

**第一次尝试**：
- ❌ npm ci 失败 - prisma generate 需要 DATABASE_URL
- ❌ npm run build 失败 - TypeScript 类型错误

**解决方案**：
1. 复制生产 .env.production 到临时目录
2. 手动执行 `NODE_ENV=production npx prisma generate`
3. 重新执行 `npm run build`

**第二次尝试**：
- ✅ npm ci 成功（prisma generate 成功）
- ✅ npm run build 成功（exit 0）

### 3.3 Build 结果

| 项目 | 结果 |
|---|---|
| Node.js | v22.22.2 ✅ |
| npm | 10.9.7 ✅ |
| TypeScript | 5.9.3 ✅ |
| Next.js | 16.2.4 ✅ |
| Prisma | 7.8.0 ✅ |
| npm ci | ✅ 成功 |
| npm run build | ✅ 成功 (exit 0) |
| BUILD_ID | ✅ 存在 (Jun 14 06:28) |

### 3.4 构建产物

```
.next/BUILD_ID: 21 bytes
Compiled successfully in 28.4s
```

**结论**: ✅ **VPS Build 验证通过**

---

## 四、数据库与 Migration 预检

### 4.1 Migration 状态

| 项目 | 结果 |
|---|---|
| 本次是否新增 migration | **否** ✅ |
| 本次是否修改 Prisma schema | **否** ✅ |
| 是否需要 prisma migrate deploy | **否** ✅ |
| 是否需要 prisma db push | **否** ✅ |
| Migration 总数 | 23 |
| Database schema | ✅ up to date |

### 4.2 Runtime DB 检查

| 项目 | 结果 |
|---|---|
| .env.production | ✅ 存在 |
| DATABASE_URL | ✅ 已设置（脱敏） |
| Prisma client | ✅ 已生成 |
| 数据库连接 | ✅ PostgreSQL 127.0.0.1:5432/bxb_prod |

### 4.3 数据库备份

**本轮无 DB 变更，正式部署前可不强制 DB 备份**，但必须有代码回滚方案。

---

## 五、部署风险清单

### 5.1 已知 Beta 缺口

| 工具 | 状态 | 备注 |
|---|---|---|
| Packing List 公司资料复用 | ❌ Beta 缺口 | UI 不支持选择器 |
| Proforma Invoice 公司资料复用 | ❌ Beta 缺口 | UI 不支持选择器 |

**说明**: 这些不阻塞代码部署，但阻塞恢复 Beta / 公开推广。

### 5.2 已知问题

| 问题 | 状态 | 备注 |
|---|---|---|
| 广告 API / Event API 500 错误 | ⚠️ 需观察 | 本地验证中出现，需生产前后观察 |
| Packing List / Proforma 不可标 Stable | ⚠️ 已知 | 公司资料复用未实现 |

### 5.3 风险评估

| 风险项 | 级别 | 说明 |
|---|---|---|
| 代码部署风险 | 🟢 低 | Build 验证通过，无 DB 变更 |
| 功能回归风险 | 🟡 中 | 需要人工验证核心功能 |
| Beta 恢复风险 | 🟡 中 | 公司资料复用未实现 |
| 回滚风险 | 🟢 低 | 无 DB 变更，回滚简单 |

---

## 六、回滚方案

### 6.1 当前生产版本记录

| 项目 | 值 |
|---|---|
| 当前生产 git commit | `a62743a` (v1.20.42.6.55-Y-R) |
| 当前实际运行版本 | `6fe3c76` (v1.20.42.6.70) |
| BUILD_ID 时间 | Jun 13 10:07 |

### 6.2 代码备份方案

**正式部署前建议备份**：

```bash
# 在 VPS 上执行
cd /home/deploy
tar -czf xixiong-saas-backup-$(date +%Y%m%d-%H%M%S).tar.gz \
  --exclude='node_modules' \
  --exclude='.next' \
  xixiong-saas/
```

### 6.3 回滚步骤

**如果新版本异常，回滚步骤**：

```bash
# 1. 停止 PM2 进程
pm2 stop xixiong-saas

# 2. 恢复备份（如果有）
cd /home/deploy
rm -rf xixiong-saas
tar -xzf xixiong-saas-backup-XXXXXXXX-XXXXXX.tar.gz

# 3. 或者从旧版本恢复
# 如果有旧版本代码包，直接解压覆盖

# 4. 重新安装依赖和构建
cd xixiong-saas
npm ci
NODE_ENV=production npx prisma generate
npm run build

# 5. 重启 PM2
pm2 restart xixiong-saas

# 6. 验证
curl -I https://jueshi.net
pm2 logs xixiong-saas --lines 50
```

### 6.4 数据库回滚

**不涉及 DB migration，因此回滚不需要 DB rollback**。

---

## 七、部署检查清单

### 7.1 部署前检查

- [ ] 确认 origin/main HEAD = `16306cb`
- [ ] 确认本地与远端一致
- [ ] 确认 VPS Build 验证通过
- [ ] 确认无新增 migration
- [ ] 确认 Database schema up to date
- [ ] 备份当前生产代码
- [ ] 确认回滚方案已准备

### 7.2 部署步骤

```bash
# 1. 备份当前生产代码
cd /home/deploy
tar -czf xixiong-saas-backup-$(date +%Y%m%d-%H%M%S).tar.gz \
  --exclude='node_modules' \
  --exclude='.next' \
  xixiong-saas/

# 2. 同步新代码（从本地或 GitHub）
# 方式 A：从本地 rsync
rsync -avz --exclude='node_modules' --exclude='.next' --exclude='.git' \
  /Users/chq/xixiong-saas/ deploy@192.129.155.149:/home/deploy/xixiong-saas/

# 方式 B：从 GitHub pull（需要配置 SSH）
cd /home/deploy/xixiong-saas
git fetch origin
git checkout 16306cb

# 3. 安装依赖
cd /home/deploy/xixiong-saas
npm ci

# 4. 生成 Prisma client
NODE_ENV=production npx prisma generate

# 5. 构建
npm run build

# 6. 重启 PM2
pm2 restart xixiong-saas

# 7. 验证
curl -I https://jueshi.net
pm2 logs xixiong-saas --lines 50
```

### 7.3 部署后验证

- [ ] 首页正常访问
- [ ] /tools 页面正常
- [ ] /workspace 登录拦截正常
- [ ] /tools/container 计算正常
- [ ] PM2 进程稳定
- [ ] 无错误日志

---

## 八、最终建议

### 8.1 是否建议进入正式部署？

**✅ 建议进入正式部署**

**理由**：
1. ✅ 远端版本确认：`16306cb`
2. ✅ VPS Build 验证通过
3. ✅ 无新增 migration
4. ✅ Database schema up to date
5. ✅ 回滚方案已准备
6. ✅ 风险级别：低

### 8.2 是否建议恢复 Beta？

**❌ 默认否**

**理由**：
- 公司资料复用未实现（Beta 缺口）
- 需要先人工验证核心功能
- 需要观察广告 API / Event API 稳定性

### 8.3 是否建议进入 S1？

**❌ 默认否**

**理由**：
- 需要先完成部署
- 需要先恢复 Beta
- 需要先人工验证

---

## 九、下一步建议

### 9.1 立即执行（需用户批准）

**正式部署**：

```bash
# 1. 备份当前生产代码
ssh deploy@192.129.155.149
cd /home/deploy
tar -czf xixiong-saas-backup-$(date +%Y%m%d-%H%M%S).tar.gz \
  --exclude='node_modules' \
  --exclude='.next' \
  xixiong-saas/

# 2. 从本地 rsync 新代码
exit
cd /Users/chq/xixiong-saas
rsync -avz --exclude='node_modules' --exclude='.next' --exclude='.git' \
  . deploy@192.129.155.149:/home/deploy/xixiong-saas/

# 3. SSH 到 VPS 执行构建和重启
ssh deploy@192.129.155.149
cd /home/deploy/xixiong-saas
npm ci
NODE_ENV=production npx prisma generate
npm run build
pm2 restart xixiong-saas

# 4. 验证
curl -I https://jueshi.net
pm2 logs xixiong-saas --lines 50
```

### 9.2 部署后验证

1. **人工验证核心功能**
   - 登录 e2e-doc-user@jueshi.net
   - 验证 Packing List 打印按钮
   - 验证 Container 计算
   - 验证 Workspace 保存/恢复

2. **观察稳定性**
   - 监控 PM2 进程
   - 检查错误日志
   - 观察广告 API / Event API

3. **决定是否恢复 Beta**
   - 确认核心功能正常
   - 确认无严重 bug
   - 确认公司资料复用为 Beta 缺口（可接受）

---

## 十、总结

### 10.1 预检完成情况

| 任务 | 状态 |
|---|---|
| 远端版本确认 | ✅ 通过 |
| 生产状态检查 | ✅ 通过 |
| VPS Build 验证 | ✅ 通过 |
| Migration 预检 | ✅ 通过 |
| 回滚方案准备 | ✅ 完成 |

### 10.2 关键指标

| 指标 | 值 |
|---|---|
| 目标 commit | `16306cb` |
| VPS Build | ✅ 成功 |
| Migration | ✅ 无变更 |
| Database | ✅ up to date |
| 风险级别 | 🟢 低 |

---

**报告完成时间**: 2026-06-14  
**报告作者**: Hermes Agent  
**版本**: v1.20.42.6.74.8  
**状态**: ✅ 预检通过，可以部署
