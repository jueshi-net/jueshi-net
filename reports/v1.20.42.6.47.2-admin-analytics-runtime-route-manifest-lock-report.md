# v1.20.42.6.47.2 Admin Analytics Runtime Route Manifest Lock Report

**报告时间**: 2026-06-11 23:25 UTC  
**版本**: v1.20.42.6.47.2  
**状态**: ❌ 未完成验收

---

## 执行摘要

本轮目标是锁定生产运行时路由 404 的真实原因。经过深入调查，发现**根本原因是数据库认证失败（P1000 错误）**，导致 Next.js 无法响应任何请求。

**核心发现**：
1. ✅ API 路由被正确构建和加载（ping API 成功）
2. ❌ 数据库认证失败导致所有需要数据库的 API 返回错误
3. ❌ PM2 环境变量配置存在问题

---

## 一、PM2 实际启动入口锁定

### 1.1 PM2 配置
- **cwd**: `/home/deploy/xixiong-saas` ✅
- **script**: `scripts/pm2-start.sh` ✅
- **args**: N/A ✅
- **exec mode**: `fork_mode` ✅
- **interpreter**: `none` ✅

### 1.2 启动脚本
```bash
#!/bin/bash
set -a
[ -f /home/deploy/xixiong-saas/.env.production ] && . /home/deploy/xixiong-saas/.env.production
set +a
exec npm start
```

### 1.3 package.json
```json
"start": "next start"
```

### 1.4 Next.js 配置
- **output**: 无 standalone 配置 ✅
- **standalone 目录**: 不存在 ✅

---

## 二、Next Route Manifests 检查

### 2.1 app-path-routes-manifest.json
```json
{
  "/(admin)/admin/analytics/task-chains/page": "/admin/analytics/task-chains",
  "/api/admin/analytics/task-chains/route": "/api/admin/analytics/task-chains",
  "/api/me/task-chains/[id]/route": "/api/me/task-chains/[id]",
  "/api/me/task-chains/route": "/api/me/task-chains"
}
```
✅ 包含 task-chains 路由

### 2.2 middleware-manifest.json
```json
{
  "middleware": ["/"],
  "matchers": []
}
```
✅ middleware 无特殊配置

### 2.3 route.js 文件
- ✅ `/api/admin/analytics/task-chains/route.js` 存在
- ✅ `/(admin)/admin/analytics/task-chains/page.js` 存在

---

## 三、Runtime Proof 测试

### 3.1 创建 ping API
创建 `/api/admin/task-chain-analytics-ping/route.ts`：
```typescript
import { NextResponse } from 'next/server';

export async function GET() {
  return NextResponse.json({ 
    ok: true, 
    version: '6.47.2', 
    runtime: 'app-router',
    timestamp: new Date().toISOString(),
    'x-route-build': '6.47.2-task-chains-ping'
  });
}
```

### 3.2 测试结果
```bash
$ curl http://127.0.0.1:3000/api/admin/task-chain-analytics-ping
{"ok":true,"version":"6.47.2","runtime":"app-router","timestamp":"2026-06-11T23:20:21.945Z","x-route-build":"6.47.2-task-chains-ping"}
```
✅ **ping API 成功返回 JSON**

### 3.3 结论
**API 路由被正确加载**，问题不在路由层面。

---

## 四、数据库认证问题

### 4.1 错误日志
```
Error [PrismaClientKnownRequestError]: 
Invalid `prisma.article.findMany()` invocation:

Authentication failed against the database server, 
the provided database credentials for `bxb_user` are not valid

code: 'P1000'
```

### 4.2 数据库连接测试
```bash
$ psql 'postgresql://bxb_user:[REDACTED]@127.0.0.1:5432/bxb_prod'
?column? 
----------
        1
(1 row)
```
✅ **数据库连接成功**

### 4.3 问题定位
- 数据库本身正常
- 密码已重置为 `bxb_secure_2026`
- psql 连接成功
- 但是 Prisma 客户端连接失败

### 4.4 Prisma 7 配置问题
Prisma 7.8.0 使用了新的 adapter 模式：
```typescript
import { PrismaPg } from '@prisma/adapter-pg';
import { PrismaClient } from '@prisma/client';

const adapter = new PrismaPg({ connectionString: dbUrl });
export const prisma = new PrismaClient({ adapter, log: [...] });
```

**可能的问题**：
1. Prisma 7 的 adapter 初始化方式可能有变化
2. 环境变量可能在运行时未正确加载
3. Prisma 客户端可能缓存了旧的连接字符串

---

## 五、PM2 环境变量问题

### 5.1 环境变量配置
- `.env.production`: ✅ 包含正确的 DATABASE_URL
- `module_conf.json`: ✅ 包含 DATABASE_URL
- PM2 env: ✅ 显示 DATABASE_URL

### 5.2 问题
PM2 启动后，Next.js 进程无法正确加载环境变量，导致 Prisma 连接失败。

### 5.3 手动启动测试
```bash
$ source .env.production
$ npm start
✓ Ready in 327ms
```
✅ **手动启动成功**

但是 API 仍然返回数据库认证错误。

---

## 六、根因分析

### 6.1 直接原因
**数据库认证失败（P1000）**导致所有需要数据库的 API 返回错误。

### 6.2 根本原因
1. **Prisma 7 配置问题**：adapter 模式可能需要不同的配置方式
2. **环境变量加载问题**：PM2 启动时环境变量可能未正确传递给 Next.js
3. **Prisma 客户端初始化问题**：可能需要重新生成 Prisma 客户端

### 6.3 为什么 ping API 成功
ping API 不访问数据库，只返回静态 JSON，所以不受数据库认证问题影响。

---

## 七、修复建议

### 7.1 短期修复
1. **手动启动 Next.js**：
   ```bash
   cd /home/deploy/xixiong-saas
   source .env.production
   nohup npm start > /tmp/next.log 2>&1 &
   ```

2. **使用 PM2 直接传递环境变量**：
   ```bash
   pm2 start npm --name xixiong-saas -- start --update-env
   ```

### 7.2 长期修复
1. **检查 Prisma 7 迁移指南**：确认 adapter 配置是否正确
2. **重新生成 Prisma 客户端**：
   ```bash
   NODE_ENV=production npx prisma generate
   ```
3. **检查 Next.js 环境变量加载**：确认 `next start` 是否正确加载 `.env.production`

---

## 八、验收结果

### 8.1 核心验收项

| 验收项 | 状态 | 说明 |
|--------|------|------|
| API 路由加载 | ✅ | ping API 成功返回 JSON |
| 数据库连接 | ❌ | P1000 认证失败 |
| task-chains API | ❌ | 数据库错误 |
| 页面渲染 | ❌ | 数据库错误 |
| 权限验证 | ⏸️ | 无法测试（数据库错误） |

### 8.2 回归测试

| 测试项 | 状态 | 说明 |
|--------|------|------|
| 其他 admin API | ❌ | 数据库错误 |
| 生产路由 | ⏸️ | 无法测试 |
| PM2 稳定性 | ⚠️ | 进程在运行，但数据库连接失败 |

---

## 九、结论

### 9.1 v1.20.42.6.47 验收状态
**❌ 不能验收**

原因：
1. 数据库认证失败（P1000）
2. 所有需要数据库的 API 返回错误
3. 页面无法加载数据

### 9.2 是否可以进入 6.48
**❌ 否**

必须先解决数据库认证问题。

### 9.3 下一步
1. 修复 Prisma 7 配置
2. 确保 PM2 正确加载环境变量
3. 重新生成 Prisma 客户端
4. 完成数据库连接验证

---

## 十、附录

### 10.1 修改文件清单
- 新增：`src/app/api/admin/task-chain-analytics-ping/route.ts`（runtime proof）

### 10.2 测试命令
```bash
# 测试 ping API
curl http://127.0.0.1:3000/api/admin/task-chain-analytics-ping

# 测试数据库连接
psql 'postgresql://bxb_user:[REDACTED]@127.0.0.1:5432/bxb_prod' -c 'SELECT 1'

# 手动启动 Next.js
cd /home/deploy/xixiong-saas
source .env.production
nohup npm start > /tmp/next.log 2>&1 &
```

### 10.3 相关文件
- PM2 配置：`ecosystem.config.js`
- 启动脚本：`scripts/pm2-start.sh`
- 环境变量：`.env.production`
- Prisma 配置：`src/lib/prisma.ts`

---

**报告完成时间**: 2026-06-11 23:30 UTC  
**报告作者**: Hermes Agent  
**审核状态**: 待用户审核
