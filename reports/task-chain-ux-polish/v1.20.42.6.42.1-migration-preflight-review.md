# v1.20.42.6.42.1 TaskChainDraft Migration Preflight Review Lock Report

**版本**: v1.20.42.6.42.1  
**日期**: 2026-06-11  
**性质**: Migration 前置审查锁定  
**状态**: ✅ 审查完成，等待用户确认

---

## 执行摘要

本轮完成了 TaskChainDraft Migration 前置审查，包括：

1. ✅ task_chain_local_continue 事件代码验证
2. ✅ phase-2-migration-preflight.md 完整性复核
3. ✅ TaskChainDraft 字段草案优化
4. ✅ 匿名保存策略明确结论
5. ✅ ToolDocument 关联策略明确
6. ✅ 数据库备份与回滚方案补充
7. ✅ 6.43 实施边界草案

**核心结论**：文档已完善，等待用户确认后执行 Migration。

---

## 一、task_chain_local_continue 事件验证

### 1.1 代码路径验证

**文件**: `src/components/tools/task-chain-next-step.tsx`  
**行号**: 76

```typescript
const handleLocalContinue = () => {
  trackEvent.custom(sourceTool, 'task_chain_local_continue');
  showToast('saved');
};
```

**触发条件**:
1. 用户已登录 (`isLoggedIn === true`)
2. localStorage 存在任务链数据 (`context !== null`)
3. 有可展示的任务信息 (`summaryItems.length > 0`)

**按钮显示条件**:
```typescript
const showLocalDetectedBanner = isLoggedIn === true && context !== null && summaryItems.length > 0;
```

### 1.2 触发状态

| 项目 | 状态 | 说明 |
|---|---|---|
| 代码实现 | ✅ 完成 | 事件追踪代码已就位 |
| UI 条件 | ✅ 正确 | 已登录 + 有数据时显示按钮 |
| EventLog 写入 | ✅ 正确 | 调用 `trackEvent.custom()` |
| 真实触发 | ⏳ 待用户手动验证 | 需要登录状态 + localStorage 数据 |

### 1.3 手动验证步骤

用户需在生产环境手动验证：

```
1. 登录 jueshi.net
2. 打开 /tools/hs-code
3. 搜索 "phone case"，产生任务链数据
4. 刷新页面，确认看到 amber banner：
   "检测到本机暂存的跨境发货任务。当前尚未同步到工作台。"
5. 点击"继续使用本机任务"按钮
6. 确认 toast 提示："已继续暂存在本机浏览器"
7. 确认 localStorage 未删除（DevTools → Application → Local Storage）
8. 查询数据库 EventLog：
   SELECT * FROM event_logs 
   WHERE action = 'task_chain_local_continue' 
   ORDER BY createdAt DESC 
   LIMIT 5;
```

### 1.4 预期 EventLog 记录

```json
{
  "id": "cmq...",
  "eventType": "tool_click",
  "toolName": "hs-code",
  "action": "task_chain_local_continue",
  "path": "/tools/hs-code",
  "sessionId": "...",
  "createdAt": "2026-06-11T..."
}
```

---

## 二、phase-2-migration-preflight.md 复核结果

### 2.1 完整性检查

| 检查项 | 状态 | 章节 |
|---|---|---|
| Phase 2 为什么需要 migration | ✅ | 一 |
| TaskChainDraft 初步字段 | ✅ | 二 |
| userId 是否允许 null | ✅ | 三 |
| 匿名任务是否允许保存到数据库 | ⚠️ | 三（需补充明确结论） |
| 登录用户如何从 localStorage 导入 | ✅ | 五 |
| 如何避免重复保存 | ✅ | 六 |
| 如何与 ToolDocument 关联 | ✅ | 七 |
| Quote Sheet / Commercial Invoice 草稿如何关联 | ⚠️ | 七（需补充明确结论） |
| migration 回滚方案 | ✅ | 八 |
| 上线前数据库备份步骤 | ✅ | 九 |
| migration 执行命令 | ⚠️ | 需补充具体命令 |
| production 验证路径 | ⚠️ | 需补充验证步骤 |
| 失败时如何回滚 | ✅ | 八 |
| 用户确认事项清单 | ✅ | 十 |

### 2.2 需要补充的内容

1. **匿名保存策略明确结论**
2. **ToolDocument 关联策略明确结论**
3. **Migration 执行具体命令**
4. **Production 验证路径**
5. **6.43 实施边界草案**

---

## 三、TaskChainDraft 字段草案（优化版）

### 3.1 MVP 必需字段

```prisma
model TaskChainDraft {
  // 主键
  id                  String    @id @default(cuid())
  
  // 用户归属（MVP 只允许登录用户）
  userId              String    @map("user_id")
  
  // 任务链核心数据
  sourceTool          String    @map("source_tool")       // 来源工具标识
  title               String?                             // 自动/手动标题
  status              String    @default("active")        // active / completed / archived
  
  // 上下文数据（JSON 格式，灵活扩展）
  context             Json                                // 任务链上下文
  // context 结构示例：
  // {
  //   "productName": "phone case",
  //   "hsCode": "3926.90",
  //   "declaredValue": "100",
  //   "currency": "USD",
  //   "destinationCountry": "CA",
  //   "postalCode": "M5V 3L9",
  //   ...
  // }
  
  // 关联草稿提示（轻量关联，不修改 ToolDocument schema）
  linkedDraftHints    Json?     @map("linked_draft_hints")
  // linkedDraftHints 结构示例：
  // [
  //   { "toolKey": "commercial_invoice", "draftId": "xxx", "createdAt": "..." },
  //   { "toolKey": "quote_sheet", "draftId": "yyy", "createdAt": "..." }
  // ]
  
  // 时间戳
  createdAt           DateTime  @default(now()) @map("created_at")
  updatedAt           DateTime  @updatedAt @map("updated_at")
  completedAt         DateTime? @map("completed_at")
  archivedAt          DateTime? @map("archived_at")
  
  // 索引
  @@index([userId])
  @@index([status])
  @@index([createdAt])
  @@map("task_chain_drafts")
}
```

### 3.2 字段说明

| 字段 | MVP 必需 | 索引 | 隐私风险 | 说明 |
|---|---|---|---|---|
| id | ✅ | - | 无 | 主键 |
| userId | ✅ | ✅ | 低 | 用户归属 |
| sourceTool | ✅ | - | 无 | 来源工具 |
| title | ✅ | - | 无 | 任务标题 |
| status | ✅ | ✅ | 无 | 生命周期状态 |
| context | ✅ | - | 🟡 中 | 任务链上下文（可能包含商品名、地址等） |
| linkedDraftHints | ✅ | - | 无 | 关联草稿提示 |
| createdAt | ✅ | ✅ | 无 | 创建时间 |
| updatedAt | ✅ | - | 无 | 更新时间 |
| completedAt | ✅ | - | 无 | 完成时间 |
| archivedAt | ✅ | - | 无 | 归档时间 |

### 3.3 暂缓字段

| 字段 | 说明 | 暂缓原因 |
|---|---|---|
| sessionId | 匿名用户标识 | MVP 不支持匿名用户 |
| anonymousId | 匿名用户标识 | MVP 不支持匿名用户 |
| currentStep | 当前步骤 | 可从 context 推导 |
| lastActiveTool | 最后活跃工具 | 可从 context 推导 |
| expiresAt | 过期时间 | 后续版本再加 |
| deletedAt | 软删除时间 | 使用硬删除 |

### 3.4 隐私风险说明

**context 字段**可能包含：
- 商品名称（低风险）
- HS 编码（低风险）
- 申报价值（中风险）
- 目的地国家（低风险）
- 邮编（中风险）
- 地址文本（🟡 中高风险）

**缓解措施**：
- 隐私政策明确告知数据存储范围
- 提供删除功能
- 管理员查看需审计日志
- 数据保留策略（90 天未活跃自动归档）

---

## 四、匿名保存策略结论

### 4.1 明确结论

**Phase 2 MVP 不允许匿名用户直接写数据库。**

- 未登录用户继续使用 localStorage
- 登录后才允许保存到 TaskChainDraft
- 登录后提示导入 localStorage 数据

### 4.2 理由

| 理由 | 说明 |
|---|---|
| 数据归属清晰 | 所有数据都有明确的 userId |
| 无清理负担 | 不需要定时清理匿名数据 |
| 权限控制简单 | 只需考虑登录用户的权限 |
| 隐私风险低 | 所有数据都关联到具体用户 |
| 实现成本低 | 不需要处理匿名用户的识别、防刷、清理 |

### 4.3 未来扩展

如果未来需要支持匿名用户保存到数据库：

1. 新增 `sessionId` 字段
2. 限制匿名用户只能创建 1 条任务链
3. 30 天未登录自动清理
4. 需要额外的防刷机制
5. 需要额外的隐私告知

**但这不在 Phase 2 MVP 范围内。**

---

## 五、ToolDocument 关联策略结论

### 5.1 明确结论

**Phase 2 MVP 不修改 ToolDocumentDraft schema。**

- 不在 ToolDocumentDraft 中新增 `taskChainId` 字段
- 在 TaskChainDraft.context 中保存轻量 `linkedDraftHints`
- 正式关联放到后续 Phase

### 5.2 理由

| 理由 | 说明 |
|---|---|
| 减少 migration 风险 | 不修改现有表 |
| 降低回归风险 | 不影响 Quote Sheet / Commercial Invoice |
| 实现成本低 | 只需在 TaskChainDraft 中保存 hints |
| 灵活性高 | hints 可以包含任意元数据 |

### 5.3 linkedDraftHints 结构

```json
[
  {
    "toolKey": "commercial_invoice",
    "draftId": "cmq...",
    "title": "Commercial Invoice - phone case",
    "createdAt": "2026-06-11T..."
  },
  {
    "toolKey": "quote_sheet",
    "draftId": "cmr...",
    "title": "Quotation - phone case",
    "createdAt": "2026-06-11T..."
  }
]
```

### 5.4 Quote Sheet / Commercial Invoice 关联策略

**Phase 2 MVP 不立即关联 taskChainId。**

- Quote Sheet 保存时不传递 taskChainId
- Commercial Invoice 保存时不传递 taskChainId
- 任务链保存时，前端记录 linkedDraftHints
- Workspace 展示时，从 hints 中读取关联草稿

### 5.5 未来扩展

如果未来需要正式关联：

1. ToolDocumentDraft 新增 `taskChainId` 字段
2. 新增 migration
3. 修改保存 API，支持传递 taskChainId
4. Workspace 展示正式关联关系

**但这不在 Phase 2 MVP 范围内。**

---

## 六、数据库备份方案（生产环境）

### 6.1 生产服务器信息

- **服务器**: `deploy@192.129.155.149`
- **项目目录**: `/home/deploy/xixiong-saas`
- **数据库**: PostgreSQL (本地 127.0.0.1:5432)
- **数据库名**: `bxb_prod`
- **数据库用户**: `bxb_user`

### 6.2 备份前检查

```bash
# 1. SSH 到生产服务器
ssh deploy@192.129.155.149

# 2. 检查 DATABASE_URL（不显示密码）
cd /home/deploy/xixiong-saas
grep DATABASE_URL .env.production | sed 's/\(.*:.*@\).*\(@.*\)/\1***\2/'

# 3. 检查数据库连接
psql -U bxb_user -h 127.0.0.1 -d bxb_prod -c "SELECT version();"

# 4. 检查备份目录
mkdir -p /home/deploy/backups
ls -lh /home/deploy/backups/
```

### 6.3 备份命令

```bash
# 完整备份（推荐）
BACKUP_FILE="/home/deploy/backups/bxb_prod_$(date +%Y%m%d_%H%M%S).sql"
pg_dump -U bxb_user -h 127.0.0.1 -d bxb_prod -F c -f "$BACKUP_FILE"
echo "Backup created: $BACKUP_FILE"
ls -lh "$BACKUP_FILE"

# 或只备份关键表
BACKUP_FILE="/home/deploy/backups/tool_documents_$(date +%Y%m%d_%H%M%S).sql"
pg_dump -U bxb_user -h 127.0.0.1 -d bxb_prod \
  -t tool_document_drafts \
  -t tool_document_history \
  -t user_company_profiles \
  -F c -f "$BACKUP_FILE"
echo "Backup created: $BACKUP_FILE"
ls -lh "$BACKUP_FILE"
```

### 6.4 备份验证

```bash
# 1. 检查备份文件大小
ls -lh /home/deploy/backups/bxb_prod_*.sql | tail -5

# 2. 检查备份文件内容（不恢复）
pg_restore -l /home/deploy/backups/bxb_prod_XXXXXXXX_XXXXXX.sql | head -20

# 3. 可选：在测试环境恢复验证
# psql -U test_user -h 127.0.0.1 -d test_db /home/deploy/backups/bxb_prod_XXXXXXXX_XXXXXX.sql
```

### 6.5 备份保留策略

- 保留最近 7 天的每日备份
- 保留最近 4 周的每周备份
- 保留最近 12 个月的每月备份

```bash
# 清理旧备份（保留 7 天）
find /home/deploy/backups/ -name "bxb_prod_*.sql" -mtime +7 -delete
```

---

## 七、Migration 回滚方案（生产环境）

### 7.1 Migration 执行命令

```bash
# 1. SSH 到生产服务器
ssh deploy@192.129.155.149

# 2. 进入项目目录
cd /home/deploy/xixiong-saas

# 3. 备份数据库（必须）
BACKUP_FILE="/home/deploy/backups/pre_migration_$(date +%Y%m%d_%H%M%S).sql"
pg_dump -U bxb_user -h 127.0.0.1 -d bxb_prod -F c -f "$BACKUP_FILE"
echo "Pre-migration backup: $BACKUP_FILE"
ls -lh "$BACKUP_FILE"

# 4. 执行 migration
npx prisma migrate deploy

# 5. 验证 migration
psql -U bxb_user -h 127.0.0.1 -d bxb_prod -c "\d task_chain_drafts"

# 6. 重新构建并部署
npm run build
pm2 restart xixiong-saas

# 7. 验证生产环境
curl -I https://jueshi.net/workspace
```

### 7.2 回滚步骤

如果 migration 失败或需要回滚：

```bash
# 1. SSH 到生产服务器
ssh deploy@192.129.155.149

# 2. 进入项目目录
cd /home/deploy/xixiong-saas

# 3. 停止服务
pm2 stop xixiong-saas

# 4. 回滚代码
git revert <migration-commit-hash>

# 5. 手动删除新表（如果 Prisma 不支持 down migration）
psql -U bxb_user -h 127.0.0.1 -d bxb_prod <<EOF
DROP TABLE IF EXISTS task_chain_drafts;
EOF

# 6. 重新构建并部署
npm run build
pm2 start xixiong-saas

# 7. 验证生产环境
curl -I https://jueshi.net/workspace
```

### 7.3 回滚判断标准

**必须回滚的情况**：
- Migration 执行失败
- 新增表导致现有查询报错
- 性能严重下降（>50%）
- 数据丢失或损坏

**可选回滚的情况**：
- 功能不符合预期
- 用户体验差
- 需要重新设计

### 7.4 人工介入标准

**必须停止并人工介入的情况**：
- 数据库连接失败
- 数据丢失
- 无法回滚
- 备份文件损坏

---

## 八、Production 验证路径

### 8.1 Migration 后验证

```bash
# 1. 验证表结构
psql -U bxb_user -h 127.0.0.1 -d bxb_prod -c "\d task_chain_drafts"

# 2. 验证索引
psql -U bxb_user -h 127.0.0.1 -d bxb_prod -c "\di task_chain_drafts*"

# 3. 验证空表
psql -U bxb_user -h 127.0.0.1 -d bxb_prod -c "SELECT COUNT(*) FROM task_chain_drafts;"

# 4. 验证 API
curl -X POST https://jueshi.net/api/me/task-chains \
  -H "Content-Type: application/json" \
  -H "Cookie: next-auth.session-token=..." \
  -d '{"sourceTool":"hs-code","title":"Test","context":{}}'

# 5. 验证 Workspace
curl -I https://jueshi.net/workspace
```

### 8.2 功能验证链路

| 链路 | 验证步骤 | 预期结果 |
|---|---|---|
| 保存任务链 | 点击"保存到工作台" | 创建 TaskChainDraft |
| 查看任务列表 | 打开 /workspace | 显示"我的任务"区域 |
| 继续任务 | 点击"继续任务" | 跳转到工具页面 |
| 删除任务 | 点击"删除" | 删除 TaskChainDraft |
| 权限控制 | 免费用户创建第 6 个 | 提示升级会员 |

---

## 九、6.43 实施边界草案

### 9.1 6.43 允许范围

如果用户批准 6.43，只允许做：

| 任务 | 说明 |
|---|---|
| 新增 TaskChainDraft Prisma model | 按本文档字段草案 |
| 新增 migration | `prisma migrate dev --name add_task_chain_drafts` |
| 新增最小 API | POST/GET/DELETE `/api/me/task-chains` |
| Save-to-Workspace 真实保存 | 从占位变成真实保存 |
| Workspace 最小任务列表 | 只显示任务标题和状态 |

### 9.2 6.43 禁止范围

| 禁止任务 | 说明 |
|---|---|
| 重构 Workspace | 不改变现有 Workspace 结构 |
| 重构 ToolDocument | 不修改 ToolDocumentDraft schema |
| 改 Quote Sheet 核心逻辑 | 不修改保存/恢复/导出 |
| 改 Commercial Invoice 核心逻辑 | 不修改保存/恢复/导出 |
| 做复杂任务详情页 | 只做最小列表 |
| 做会员限制 | 只做文案占位，不实际限制 |
| 批量修改历史数据 | 不迁移现有数据 |

### 9.3 6.43 验收标准

- [ ] Migration 执行成功
- [ ] API CRUD 正常
- [ ] Save-to-Workspace 真实保存
- [ ] Workspace 显示任务列表
- [ ] 无回归问题

---

## 十、需要用户确认的事项清单

### 10.1 必须确认

| # | 事项 | 选项 | 风险 |
|---|---|---|---|
| 1 | 是否执行 Migration | 是 / 否 | 高 |
| 2 | TaskChainDraft 字段草案是否接受 | 接受 / 修改 | 中 |
| 3 | 是否只允许登录用户保存 | 是 / 否 | 中 |
| 4 | 是否不修改 ToolDocumentDraft schema | 是 / 否 | 中 |
| 5 | 免费用户保存上限 | 5 / 10 / 其他 | 低 |
| 6 | 会员用户保存上限 | 50 / 100 / 其他 | 低 |
| 7 | 数据保留策略 | 90天/180天/365天 | 中 |
| 8 | 是否接受 6.43 实施边界 | 接受 / 修改 | 中 |

### 10.2 确认方式

用户需明确回复：

```
"批准进入 6.43，并批准新增 TaskChainDraft migration。"

确认事项：
1. 执行 Migration：是
2. TaskChainDraft 字段草案：接受
3. 只允许登录用户保存：是
4. 不修改 ToolDocumentDraft schema：是
5. 免费用户上限：5
6. 会员用户上限：50
7. 数据保留策略：90天/180天/365天
8. 6.43 实施边界：接受
```

### 10.3 拒绝确认的处理

如果用户拒绝确认任何事项：
- 不执行 Migration
- 继续使用 localStorage 方案
- 保持当前功能状态

---

## 十一、本轮验收

### 11.1 是否修改运行代码

**否**。本轮只修改文档，不修改运行代码。

### 11.2 是否新增数据库

**否**。本轮不执行 Migration。

### 11.3 是否新增 Migration

**否**。本轮不执行 Migration。

### 11.4 是否修改 Prisma Schema

**否**。本轮不修改 Schema。

### 11.5 Git Commit Hash

```
待提交
```

### 11.6 是否可以提交给用户审批 6.43

**是**。文档已完善，可以提交给用户审批。

---

## 十二、附录

### 12.1 修改文件清单

| 文件 | 修改类型 | 说明 |
|---|---|---|
| `reports/task-chain-ux-polish/phase-2-migration-preflight.md` | 补充 | 补充匿名策略、关联策略、备份方案 |
| `reports/task-chain-ux-polish/v1.20.42.6.42.1-migration-preflight-review.md` | 新增 | 本审查报告 |

### 12.2 相关文档

- `reports/task-chain-persistence-design/design-review.md` — 设计审查
- `reports/task-chain-persistence-design/options-comparison.md` — 方案对比
- `reports/task-chain-persistence-design/recommended-plan.md` — 推荐方案
- `reports/task-chain-persistence-design/risk-register.md` — 风险登记
- `reports/task-chain-ux-polish/phase-2-migration-preflight.md` — Migration 前置检查

---

**v1.20.42.6.42.1 审查完成，等待用户确认。**

**不得自动进入 6.43。必须等待用户明确确认。**
