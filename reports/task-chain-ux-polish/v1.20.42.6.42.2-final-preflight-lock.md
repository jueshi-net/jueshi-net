# v1.20.42.6.42.2 Migration Preflight Final Fix & Event Proof Report

**版本**: v1.20.42.6.42.2  
**日期**: 2026-06-11  
**性质**: 最终 migration preflight 修正与事件证据补齐  
**状态**: ✅ 完成，等待用户确认

---

## 执行摘要

本轮完成了：

1. ✅ 发现并修复 `useIsLoggedIn()` 前端 bug（httpOnly cookie 问题）
2. ✅ 真实触发并验证 `task_chain_local_continue` 事件（2 条数据库记录）
3. ✅ 修正 TaskChainDraft 字段草案（completedAt/archivedAt 改为可选）
4. ✅ 修正备份命令（.dump vs .sql）
5. ✅ 修正回滚方案（rsync 而非 git revert）
6. ✅ 修正 6.43 实施边界（收紧范围）
7. ✅ 修正用户确认事项清单（12 项最终清单）

**核心成果**：前端 bug 已修复，事件证据已补齐，文档已完善，等待用户确认。

---

## 一、是否修改运行代码

**是**。修复了 `useIsLoggedIn()` 前端 bug。

### 1.1 Bug 描述

**问题**：`useIsLoggedIn()` 使用 `document.cookie.includes('next-auth.session-token')` 检测登录状态，但 next-auth 的 session cookie 是 httpOnly（安全最佳实践），因此 `document.cookie` 无法读取，导致 hook 始终返回 false。

**影响**：
- "继续使用本机任务" 按钮永远不会显示
- 已登录用户无法触发 `task_chain_local_continue` 事件
- amber banner "检测到本机暂存的跨境发货任务" 不会显示

### 1.2 修复方案

将 `useIsLoggedIn()` 从检查 cookie 改为调用 `/api/auth/session` 端点：

```typescript
// 修复前（错误）
function useIsLoggedIn(): boolean | null {
  const [isLoggedIn, setIsLoggedIn] = useState<boolean | null>(null);
  useEffect(() => {
    const hasSession = typeof document !== 'undefined' && (
      document.cookie.includes('next-auth.session-token') ||
      document.cookie.includes('__Secure-next-auth.session-token')
    );
    setIsLoggedIn(hasSession);
  }, []);
  return isLoggedIn;
}

// 修复后（正确）
function useIsLoggedIn(): boolean | null {
  const [isLoggedIn, setIsLoggedIn] = useState<boolean | null>(null);
  useEffect(() => {
    let cancelled = false;
    fetch('/api/auth/session', { credentials: 'include' })
      .then(res => {
        if (cancelled) return null;
        if (!res.ok) return null;
        return res.json();
      })
      .then(data => {
        if (cancelled) return;
        setIsLoggedIn(!!data?.user);
      })
      .catch(() => {
        if (!cancelled) setIsLoggedIn(false);
      });
    return () => { cancelled = true; };
  }, []);
  return isLoggedIn;
}
```

### 1.3 修改范围

| 文件 | 修改类型 | 说明 |
|---|---|---|
| `src/components/tools/task-chain-next-step.tsx` | 修改 | 修复 `useIsLoggedIn()` hook |

### 1.4 构建与部署

| 步骤 | 结果 |
|---|---|
| 本地 `npm run build` | ✅ 通过 |
| rsync → VPS | ✅ 完成 |
| VPS `rm -rf .next && npm run build` | ✅ 通过 |
| `pm2 restart xixiong-saas` | ✅ online (PID 1283235, 64MB) |

---

## 二、task_chain_local_continue 真实触发结果

### 2.1 触发过程

1. 登录 jueshi.net（test@jueshi.net）
2. 设置 localStorage：`jueshi.taskChain.shippingMvp`
3. 导航到 `/tools/hs-code`
4. 确认看到 amber banner 和 "继续使用本机任务" 按钮
5. 点击按钮
6. 确认 toast 提示："已继续暂存在本机浏览器"
7. 查询数据库 EventLog

### 2.2 UI 验证

| 项目 | 状态 | 说明 |
|---|---|---|
| 登录状态检测 | ✅ | `/api/auth/session` 返回 `{ user: { email: "test@jueshi.net" } }` |
| localStorage 数据 | ✅ | `jueshi.taskChain.shippingMvp` 存在 |
| amber banner | ✅ | "检测到本机暂存的跨境发货任务。当前尚未同步到工作台。" |
| "继续使用本机任务" 按钮 | ✅ | 可见且可点击 |
| toast 提示 | ✅ | "已继续暂存在本机浏览器" |
| localStorage 保留 | ✅ | 数据未被删除 |

### 2.3 数据库记录

查询命令：
```sql
SELECT id, "eventType", "toolName", action, path, "createdAt"
FROM event_logs
WHERE action LIKE '%task_chain_local_continue%'
ORDER BY "createdAt" DESC
LIMIT 5;
```

结果：
```json
[
  {
    "id": "cmq8zzbkt00036w5ph91k10kk",
    "eventType": "tool_click",
    "toolName": "hs-code",
    "action": "task_chain_local_continue",
    "path": "/tools/hs-code",
    "createdAt": "2026-06-11T04:28:34.781Z"
  },
  {
    "id": "cmq8zt2660003n85p9ugrdwjs",
    "eventType": "tool_click",
    "toolName": "hs-code",
    "action": "task_chain_local_continue",
    "path": "/tools/hs-code",
    "createdAt": "2026-06-11T04:23:42.654Z"
  }
]
```

**结论**：✅ 事件成功写入数据库，共 2 条记录（1 条手动触发 + 1 条按钮点击）。

---

## 三、TaskChainDraft 字段草案修正结果

### 3.1 修正内容

**修正前**：completedAt / archivedAt 标记为 MVP 必需

**修正后**：completedAt / archivedAt 改为 MVP 可选

### 3.2 MVP 必需字段（9 个）

| 字段 | 索引 | 隐私风险 | 说明 |
|---|---|---|---|
| id | PK | 无 | 主键 |
| userId | ✅ | 低 | 用户归属（必填） |
| sourceTool | - | 无 | 来源工具 |
| title | - | 无 | 任务标题 |
| status | ✅ | 无 | 生命周期状态 |
| context | - | 🟡 中 | 任务链上下文 |
| linkedDraftHints | - | 无 | 关联草稿提示 |
| createdAt | ✅ | 无 | 创建时间 |
| updatedAt | - | 无 | 更新时间 |

### 3.3 MVP 可选字段（3 个）

| 字段 | 索引 | 隐私风险 | 说明 |
|---|---|---|---|
| completedAt | - | 无 | 只有任务完成后才有值 |
| archivedAt | - | 无 | 只有归档后才有值 |
| lastActiveTool | - | 无 | 最后活跃工具（冗余存储更快） |

### 3.4 修正理由

- completedAt 只有任务完成后才有，MVP 阶段不需要
- archivedAt 只有归档后才有，MVP 阶段不需要
- 生命周期结束字段不应强制要求

---

## 四、备份命令修正结果

### 4.1 修正内容

**修正前**：`-F c` (custom format) 使用 `.sql` 扩展名

**修正后**：`-F c` (custom format) 必须使用 `.dump` 扩展名

### 4.2 修正后的备份命令

```bash
# 完整备份（推荐，custom format）
mkdir -p /home/deploy/backups
BACKUP_FILE="/home/deploy/backups/bxb_prod_$(date +%Y%m%d_%H%M%S).dump"
pg_dump "$DATABASE_URL" -F c -f "$BACKUP_FILE"
echo "Backup created: $BACKUP_FILE"
ls -lh "$BACKUP_FILE"

# 验证备份成功（文件大小应 > 0）
if [ ! -s "$BACKUP_FILE" ]; then
  echo "ERROR: Backup file is empty or missing. DO NOT proceed with migration."
  exit 1
fi

# 验证备份内容（列出表）
pg_restore -l "$BACKUP_FILE" | head -20
```

### 4.3 安全规则

- 不得把真实数据库密码写入报告或日志
- 不得在日志中 echo DATABASE_URL
- 备份失败（文件大小为 0）时，**不得执行 migration**
- 必须先 `mkdir -p /home/deploy/backups`

---

## 五、回滚方案修正结果

### 5.1 修正内容

**修正前**：使用 `git revert <migration-commit-hash>`

**修正后**：使用 rsync 从本地稳定版本回滚

### 5.2 修正理由

VPS 通过 rsync 部署（exclude .git），因此 VPS 上没有 git 历史。`git revert` 无法在 VPS 上执行。

### 5.3 修正后的回滚步骤

```bash
# === 代码回滚（在本地 Mac 执行）===

# 1. 在本地回退到上一个稳定 commit
cd /Users/chq/xixiong-saas
git log --oneline -5  # 找到 migration 之前的 commit
git checkout <stable-commit-hash>  # 或直接 git revert <migration-commit>

# 2. rsync 稳定版本到 VPS
rsync -az --exclude='node_modules' --exclude='.next' --exclude='.git' \
  ./ deploy@192.129.155.149:/home/deploy/xixiong-saas/

# === 数据库回滚（在 VPS 执行）===

# 3. SSH 到 VPS
ssh deploy@192.129.155.149
cd /home/deploy/xixiong-saas

# 4. 停止服务
pm2 stop xixiong-saas

# 5. 数据库回滚
# 情况 A：新表无有效用户数据 → 直接 DROP
psql "$DATABASE_URL" -c "DROP TABLE IF EXISTS \"task_chain_drafts\";"

# 情况 B：新表已有用户数据 → 先导出再 DROP
psql "$DATABASE_URL" -c "COPY task_chain_drafts TO '/home/deploy/backups/task_chain_drafts_rollback_$(date +%Y%m%d_%H%M%S).csv' WITH CSV HEADER;"
psql "$DATABASE_URL" -c "DROP TABLE IF EXISTS \"task_chain_drafts\";"

# 情况 C：如果 Prisma migration 已记录但需要回滚
psql "$DATABASE_URL" -c "DELETE FROM _prisma_migrations WHERE migration_name LIKE '%add_task_chain%';"

# 6. 重新构建并部署
rm -rf .next
npm run build
pm2 start xixiong-saas
pm2 status

# 7. 验证生产环境
curl -I https://jueshi.net/workspace
```

### 5.4 回滚判断标准

**可以自动回滚的情况**：
- Migration 执行成功但功能不符合预期
- 新增表无用户数据
- 前端 UI 显示异常但不影响其他功能

**必须回滚的情况**：
- Migration 执行失败
- 新增表导致现有查询报错
- 性能严重下降（>50%）
- 数据丢失或损坏

**必须停止并人工介入的情况**：
- 数据库连接失败
- 数据丢失且无备份
- 备份文件损坏无法恢复
- rsync 回滚后 build 失败
- 不确定新表是否有有效用户数据

---

## 六、ToolDocument 命名复核结果

### 6.1 复核方法

检查 Prisma schema 中的真实模型名：

```bash
grep "model ToolDocument" prisma/schema.prisma
```

### 6.2 复核结果

```
model ToolDocumentDraft {
model ToolDocumentHistory {
```

### 6.3 结论

**真实模型名是 `ToolDocumentDraft`**（不是 `ToolDocument`）。

文档中使用的 `ToolDocumentDraft` 是正确的，无需修改。

---

## 七、6.43 实施边界修正结果

### 7.1 修正内容

**修正前**：允许范围较宽泛

**修正后**：收紧允许范围，增加禁止项

### 7.2 允许范围（收紧后）

| 任务 | 说明 |
|---|---|
| 新增 TaskChainDraft Prisma model | 按本文档字段草案（MVP 必需字段） |
| 新增 migration | `prisma migrate dev --name add_task_chain_drafts` |
| 新增最小 API | POST `/api/me/task-chains`（保存） |
| | GET `/api/me/task-chains`（获取我的任务链） |
| | DELETE 或 PATCH archive `/api/me/task-chains/[id]` |
| Save-to-Workspace 真实保存 | 从占位变成真实保存（调用新 API） |
| Workspace 极简入口 | 可以只加一个极简入口或极简列表，但不得重构 |

### 7.3 禁止范围（增加项）

| 禁止任务 | 说明 |
|---|---|
| 重构 Workspace | 不改变现有 Workspace 结构 |
| 重构 ToolDocument | 不修改 ToolDocumentDraft schema |
| 修改 Quote Sheet 核心保存、恢复、导出逻辑 | 零影响 |
| 修改 Commercial Invoice 核心保存、恢复、导出逻辑 | 零影响 |
| 复杂任务详情页 | 只做最小列表 |
| **多设备实时同步** | 不做（新增） |
| 会员限制真实拦截 | 除非用户另行批准，否则只做文案占位 |
| 批量修改历史数据 | 不迁移现有数据 |
| **匿名用户写数据库** | 不允许，未登录用户继续只用 localStorage（新增） |

### 7.4 验收标准（增加项）

- [ ] Migration 执行成功
- [ ] API CRUD 正常
- [ ] Save-to-Workspace 真实保存（写数据库）
- [ ] Workspace 显示极简任务列表或入口
- [ ] 无回归问题（Quote Sheet / Commercial Invoice / Workspace 现有功能）
- [ ] **未登录用户点击"保存到工作台"仍提示登录**（新增）
- [ ] **登录用户保存后，数据库有对应记录**（新增）

---

## 八、用户确认事项最终清单

### 8.1 12 项确认事项

| # | 事项 | 默认值 | 风险 |
|---|---|---|---|
| 1 | 是否批准进入 6.43？ | 待确认 | 高 |
| 2 | 是否批准新增 TaskChainDraft Prisma model？ | 待确认 | 高 |
| 3 | 是否批准执行 migration？ | 待确认 | 高 |
| 4 | 是否确认 Phase 2 只允许登录用户保存？ | 是 | 中 |
| 5 | 是否确认未登录用户继续只用 localStorage？ | 是 | 中 |
| 6 | 是否确认不修改 ToolDocumentDraft schema？ | 是 | 中 |
| 7 | 是否确认免费用户任务链保存上限为 5？ | 5 | 低 |
| 8 | 是否确认会员用户任务链保存上限为 50？ | 50 | 低 |
| 9 | 是否确认数据保留策略先采用 180 天？ | 180 天 | 中 |
| 10 | 是否确认 6.43 不做复杂 Workspace 重构？ | 是 | 中 |
| 11 | 是否确认上线前必须 pg_dump 备份成功？ | 是 | 高 |
| 12 | 是否确认备份失败则不得执行 migration？ | 是 | 高 |

### 8.2 确认方式

用户需明确回复：

```
"批准进入 6.43，并批准新增 TaskChainDraft migration。"

确认事项：
1. 进入 6.43：是
2. 新增 TaskChainDraft model：是
3. 执行 migration：是
4. 只允许登录用户保存：是
5. 未登录用户只用 localStorage：是
6. 不修改 ToolDocumentDraft schema：是
7. 免费用户上限：5
8. 会员用户上限：50
9. 数据保留策略：180 天
10. 不做复杂 Workspace 重构：是
11. 上线前 pg_dump 备份必须成功：是
12. 备份失败不得执行 migration：是
```

---

## 九、本轮验收

### 9.1 是否修改运行代码

**是**。修复了 `useIsLoggedIn()` 前端 bug。

### 9.2 是否新增数据库

**否**。本轮不执行 Migration。

### 9.3 是否新增 Migration

**否**。本轮不执行 Migration。

### 9.4 是否修改 Prisma Schema

**否**。本轮不修改 Schema。

### 9.5 Git Commit Hash

```
待提交
```

### 9.6 是否可以提交给用户审批 6.43

**是**。文档已完善，事件证据已补齐，可以提交给用户审批。

---

## 十、附录

### 10.1 修改文件清单

| 文件 | 修改类型 | 说明 |
|---|---|---|
| `src/components/tools/task-chain-next-step.tsx` | 修改 | 修复 `useIsLoggedIn()` hook |
| `reports/task-chain-ux-polish/phase-2-migration-preflight.md` | 修改 | 修正字段草案、备份命令、回滚方案、6.43 边界、用户确认清单 |
| `reports/task-chain-ux-polish/v1.20.42.6.42.2-final-preflight-lock.md` | 新增 | 本报告 |

### 10.2 相关文档

- `reports/task-chain-persistence-design/design-review.md` — 设计审查
- `reports/task-chain-persistence-design/options-comparison.md` — 方案对比
- `reports/task-chain-persistence-design/recommended-plan.md` — 推荐方案
- `reports/task-chain-persistence-design/risk-register.md` — 风险登记
- `reports/task-chain-ux-polish/phase-2-migration-preflight.md` — Migration 前置检查
- `reports/task-chain-ux-polish/v1.20.42.6.42.1-migration-preflight-review.md` — 6.42.1 审查报告

---

**v1.20.42.6.42.2 完成，等待用户确认。**

**不得自动进入 6.43。必须等待用户明确确认。**
